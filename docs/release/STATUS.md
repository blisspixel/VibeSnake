# Current Status

Snapshot date: 2026-08-09

## Executive assessment

Vibe Snake is a substantial alpha with a reliable engineering baseline on GitHub. The canonical repository is [blisspixel/VibeSnake](https://github.com/blisspixel/VibeSnake): a single `main` branch, open PR list kept empty, and green hosted CI on every push.

**Product path:** Godot 4.7.1 .NET + pure C# rules/persistence. Hosted runners export and smoke native players outside the checkout on Windows, macOS, and Linux **without Python**. All nine powers are complete pure C# contracts; the Godot shell renders markers, HUD, multi-power feedback, Slow-Mo/Boost cadence, achievements unlocks/browse, and logical input.

**Python/Pygame:** Frozen behavior oracle and temporary source-playable alpha. Not the 1.0 runtime. Dual-runtime JSON fixtures exist only so C# can prove parity with the oracle. New player features belong in `native/` and `game/`, not in expanding Python gameplay.

The public checkout still includes the eight-station offline radio library (95 tracks), adaptive Python presentation, and a reference player path (`vibesnake play|update|status|doctor|version`). Continuous [player-latest](https://github.com/blisspixel/VibeSnake/releases/tag/player-latest) packages rebuild the **reference** player from `main`. Pack export approval (`exportEligible` still zero), protected platform signing, selected-store integration, physical-controller evidence, HW frame evidence, and structured playtesting remain open.

### What is next (and why)

See [ROADMAP.md Product path](../../ROADMAP.md#product-path-read-this-first). Short form:

1. **Godot shell depth** - physical controller/audio-device evidence, retained scaling screenshots, authored cues, and feel review (automated fallback-audio stress and recovery now pass).
2. **Protected signing and selected-store integration (V030-10)** - deterministic qualification archives and depot shapes exist, but they are deliberately not publishable.
3. **First export-eligible packs (V030-08/09)** - inventory is classified; ship packs are empty by gate.
4. **Structured human playtests (V070-06)** - execute the qualified four-cohort, three-stage protocol; its automated handoff is ready but honestly records zero human sessions.
5. **Power-up human decision gate (V070-09)** - run the six qualified synergy scenarios, evaluate route-decision quality, and make the evidence-backed keep/remove decision for the default-off Mutation Fork prototype. The automated C#/Godot foundation is complete.
6. **Progression human review (V080-04)** - the automated progression, cosmetic-isolation, and finite Broadcast Tour foundation is complete. Review unlock pacing, attachment, rivalry appeal, and quiet/maximum-effect visuals with real players.
7. **Replay human/platform review (V080-01)** - automated metadata, status badges, speed, HUD, export, and exact deletion consent are complete; retained multi-platform pixels and accessibility review remain.
8. **Localization visible review (V080-07)** - the 509-ID, 67-template, thirteen-flow pseudo-locale, glyph, maximum-text-scale, typed feedback, onboarding-ID, and broadcast-ID automated foundation is green with zero remaining audited presentation expressions; retain keyboard and controller-family review on Windows, macOS, and Linux.
9. **Capture and sharing review (V080-08)** - clean keyboard/controller capture and privacy-safe run-summary export are automated; retain platform captures and review the final trailer composition.
10. **Interactive spectator review (V080-09)** - the ten-rival equal-rules player flow, local league, authored commentary, recovery, and exact-seed challenge foundation is complete; retain physical controller-family, multi-platform presentation, editorial, pacing, and entertainment review.
11. **Optional lore review (V080-10)** - the 41-entry three-depth offline archive and raw keyboard/controller routes are complete; retain canon, tone, humor, maximum-text-scale multi-platform, and curiosity-pacing review.
12. **Offline comparison review (V080-11)** - stable seed codes, four verified household slots, equal-rules ghosts, private run cards, exact deletion, and raw keyboard/controller routes are complete; retain household handoff, maximum-text-scale platform, and live ghost readability review.
13. **Candidate freeze activation (V090-01)** - the machine-checkable six-surface policy and deterministic SHA-256 baseline builder are ready and green in CI, but remain explicitly inactive until every 0.8 acceptance dependency closes.
14. **Protected release matrix operations (V090-02)** - the three unsigned native jobs now cross-bind into one exact matrix decision; clean tagged Release execution, Windows signing, macOS signing/notarization/stapling, Linux runtime and desktop integration, final provenance, and channel approval remain.
15. **Install lifecycle completion (V090-03)** - exported-player preflight now covers the eight supported legacy save fixtures, future-schema preservation, read-only and non-ASCII paths, repair, optional-pack and player-data recovery, and application removal; retained three-platform execution, selected-channel installer lifecycle, and a real cross-version binary rollback remain.
16. **Reliability completion (V090-04)** - Release CI is configured for 100 clean fresh-profile exported-player launches per platform; packaged smoke adds 100,000 mirrored balanced-AI steps per ruleset, 100 spectator restarts with exact resource samples, all seven roadmap fault classes, and privacy-safe crash and first-divergence triage probes. The aggregate requires 300 launches, 600,000 compared steps, 300 restarts, 21 fault rows, and both triage records on all three platforms. Retained hosted Release execution is the remaining gate.
17. **Performance completion (V090-05)** - packaged smoke measures three exact effects profiles with 40 live frames each, reviewed shared-host ceilings, complete stress geometry, fixed presentation budgets, and rules-speed isolation. The aggregate requires 360 samples and one rules hash across the platform matrix. Named minimum and recommended hardware, both target resolutions, long-session resource evidence, and specification publication remain.
18. **Accessibility completion (V090-06)** - packaged smoke cross-binds twelve exact accessibility areas from seven SHA-256-bound evidence records, including independent remapping, single-action keyboard/controller routes, separated audio, mono output, multimodal alternatives, reduced motion, flash safety, and 150 percent text across eight display classes. The aggregate requires 24 maximum-text-scale display rows. Retained platform captures, physical devices, accessibility-user participation, and human focus/contrast/readability/photosensitivity review remain.
19. **Manual product matrix (V090-07)** - a closed 4-platform, 36-flow, 144-cell contract now validates exact candidate identity, Windows, both macOS Universal architectures against one shared Universal artifact hash, Linux, keyboard, mouse, Xbox-layout and PlayStation-layout controllers, eight audio/accessibility profiles, existing retained evidence files, and release-blocking results. Native mouse menu/settings/gameplay/Back routes now pass packaged smoke. Retained physical sessions remain at zero, so release acceptance remains false.
20. **Controlled external validation (V090-08)** - a closed 4-cohort, 3-artifact-platform handoff validates clean candidate revisions, exact artifact hashes, clean-install and repository-exposure state, keyboard/mouse/both controller layouts, six fresh-participant comprehension checks, four structured report families, retained evidence files, finding closure, and every affected-gate rerun. Candidate, session, and finding counts remain zero, so external validation and release acceptance remain false.
21. **Release materials (V090-09)** - ten required document foundations now exist for product, player, accessibility, privacy, support, recovery, known issues, third-party notices, credits, and changes. A strict candidate gate requires exact document and retained-media hashes, all platform OS and size disclosures, physical input evidence, core and optional-content sizes, save locations, six screenshot roles, two video roles, and eight evidence-linked claims. No candidate record is supplied, so candidate materials and release acceptance remain false.
22. **Release and rollback rehearsal (V090-10)** - a closed 3-platform, 11-operation, 33-cell contract validates staged candidate and previous artifacts, manifests, release-material decision, migration fixtures, signature/install/update/rollback/removal results, protected user-data identity, withdrawal, and publish/halt/replace/communicate roles through retained SHA-256 evidence. No staged rehearsal record exists, so rehearsal and release acceptance remain false.
23. **Stable 1.0 promotion** - a final guard requires ten accepted upstream decisions for one revision, the protected `1.0.0` tag rebuild, all three public artifacts plus manifests/provenance/checksums, a separate approved optional pack, public-file install smokes, seven preserved evidence categories, and six exact compatibility promises. No promotion record exists, so stable promotion and release acceptance remain false.
24. **Human gates** - HW p50/p95/p99, feel, physical input devices, recovery comprehension, accessibility review, and fresh validation.

## Verified quality baseline

| Area | Verified state |
| --- | --- |
| Version | 0.2.1 alpha |
| Canonical remote | [blisspixel/VibeSnake](https://github.com/blisspixel/VibeSnake), sole branch `main` |
| Ship runtime (target) | Godot 4.7.1 .NET + pure C# rules; packaged player without Python |
| Oracle / alpha checkout | Python 3.11-3.14 + Pygame CE 2.5.7+ (reference only) |
| Player delivery | Native export smokes on CI; reference `vibesnake update` + floating `player-latest` source/wheel packages from `main` |
| Hosted CI | Python oracle matrix (3.11-3.14), native rules on Windows/macOS/Linux, and native Godot player export smoke on all three platforms pass on tip of `main` |
| Python deterministic tests | 533 passing locally on Python 3.14; hosted CI runs the supported 3.11 through 3.14 matrix |
| Python line coverage | 87.12 percent locally with an 80 percent floor enforced by configuration and CI |
| Native toolchain | Godot 4.7.1 Mono and .NET SDK 10.0.302 pinned and verified |
| Native contract tests | **892** passing on .NET 10 with 90 percent line and branch floors per measured module; Rules measures 95.77/90.50 percent line/branch, Persistence measures 94.14/90.40 percent, creator validation measures 95.98/94.00 percent, and aggregate native coverage is 94.79/90.47 percent |
| Cross-language parity | 100 movement cases (25,600 steps), 35 core-rule cases, 8 Shield, 6 Phase Shift, 5 Last Stand, 9 remaining-power, and 4 achievement-candidate product-path cases pass |
| Godot integration | Headless import plus seeded rules, restoration, logical input with schema-1 keyboard/controller remap capture and conflict resolution, required keyboard/D-pad/stick cadence, scaled mouse menu/settings/gameplay/Back routes, and exact eight-case VirtualViewport evidence including 4:3, a 6-section keyboard/controller settings screen, centralized font/palette ownership with contrast-qualified family-aware vector prompt badges, 150-percent layout measurement, non-color focus/state markers, keyboard/controller catalog pagination, immediate isolated multi-bus volumes, focus-loss and last-controller-disconnect pause, 31 finite fallback cues with distinct combo/restart/achievement/death/power identities and measured peak/provenance/license evidence, bounded SFX/UI voices, cooldown, priority, interruption, music ducking, output-topology repair, and retained rapid-retrigger/mute/failure/backoff/recovery/cache/cleanup evidence through the Dummy backend, full nine-power markers and captions, cadence-aware stepping, onboarding, ordered run-end/personal-best presentation, verified separated player-data reset/recovery, achievements load/save and catalog browse, live replay recording, metadata/status browser, 0.5x-4x playback, HUD toggle, atomic export, exact two-step deletion consent, ten-rival equal-rules spectator selection/playback/league/challenge flow, isolated atomic saves, and clean shutdown smoke on hosted runners |
| Native artifacts | Windows, macOS, and Linux player smokes run outside the checkout on matching hosted runners **without Python**; each platform gate launches from a read-only install with fresh external user-data/log paths, exercises spaces and non-ASCII characters, requires exact before/after installed-file hashes, and emits artifact-linked unsigned signing-readiness evidence. Release lifecycle preflight adds repair identity and launch, seven source-preserving legacy save migrations, future-schema preservation, optional-pack and data recovery, and clean application removal. Packaged smoke adds 200,000 deterministic comparisons, 100 spectator restarts, seven fault classes, and crash/divergence triage. `release-matrix-qualification-v1` cross-binds all three source, build, state, dependency, manifest, signing, package, install, lifecycle, reliability, and fault identities before tag/manual provenance can run. Protected signing, selected-channel lifecycle, binary rollback, and final platform operations remain pending; continuous Python reference player-latest packages still publish from `main`. |
| Static policy | Ruff, source-policy including credential-material exclusion, documentation links, screenshot fingerprint, logo and badge hashes, content inventory, dependency locks, lock-derived dependency/provenance inventory, strict release signing routes, and the inactive 116-file candidate-freeze boundary are CI gates |
| Dependency integrity | Universal Python 3.11 through 3.14 hash-locked graph; locked NuGet restore with audit |
| Public content inventory | 114 classified files totaling 340,378,770 bytes (95 radio MP3 tracks, 9 PNG images, 7 JSON, 3 Markdown); all rights-cleared; 0 export-eligible for native packs until pack quality gates pass |
| Documentation links | Local checker in `scripts/check_docs.py` |

## Feature status

| System | Status | Evidence and qualification |
| --- | --- | --- |
| Core movement | Working | Four-direction movement, queued input, self-collision, phase overlap, and edge wrapping are implemented and tested. |
| Classic and Vibe modes | Automated contract foundation complete | `classic@1` and `vibe@1` declare exact feature, board, pause, seed, restart, description, difficulty/DDA policy, and score categories. Classic uses fixed 10-point food scoring with no hunger, combo/speed/length bonus, near miss, power, progression score, or adaptation. Vibe retains the full pressure, scoring, power, progression, and feedback model with default-on `vibe-bounded-hunger-v1`; settings can disable it for an isolated unranked category. Remappable left/right keyboard, D-pad, or stick input selects modes, confirm starts, and restart retains the same config with a fresh seed. |
| Scoring | Working | Base points, speed bonus, length bonus, bonus points, and smoothly interpolated 1x to 10x combos are implemented. Native near-miss awards are on by default via `RunConfig.EnableNearMiss` after dual-runtime fixture regen. |
| Starvation | Automated multimodal foundation complete | The base 600-tick hunger budget progresses through named safe, low, critical, and empty states with color roles and 12-segment shape changes. Vibe DDA may apply disclosed zero-to-two-tick drain per step; DDA-off drains exactly one. The warning cue, food rescue, move-then-starve order, Last Stand recovery, death telemetry, and player-run finalization are wired and tested. Pressure readability remains a human gate. |
| Menus and overlays | Working | Fifteen game states render headlessly; menu navigation is tested; retro-modern title, settings, spectator, offline comparisons, and pause chrome use adaptive framing. |
| Optional onboarding | Automated foundation complete | A missing `onboarding.json` remains on the title menu. Help opens an optional keyboard/controller offer for eight deterministic interactive lessons or immediate play. Turning, reversal rejection, wrapping, food/score, starvation, Shield, pause, and restart use unscored rules micro-scenarios with active-device prompts. Skip, completion, replay, and isolated reset persist atomically; tutorial sessions cannot write competitive scores, achievements, or replays. Human comprehension review remains. |
| Run end and bare loop | Automated foundation complete | Collision and starvation end states show ordered attribution, distinct `[X]` collision or `[0]` empty-meter geometry, recovery guidance, score, fair-category personal best, run statistics, unlocks, replay status, and rules identity. Both causes retain text plus geometry under mute, reduced motion, and flash-free play. Confirm-only restart rejects the terminal input sequence and passes later keyboard/controller intent. Bare-loop evidence locks response, buffer, 3:1 visibility, wrap, host-smoke pacing, death, restart, and reset budgets plus six semantic aspect/accessibility frames. Physical-device feel and retained platform pixels remain pending. |
| Adaptive presentation | Automated native foundation complete; platform review pending | The native 1280 by 720 logical canvas scales without stretching across minimum, 4:3, 16:9, 16:10, square, ultrawide, 4K, and 5K surfaces. Settings expose a 1024 by 768 classic 4:3 window, three modern desktop presets, windowed mode, borderless fullscreen, and exclusive fullscreen. Pointer transforms reject letterbox and pillarbox regions. Retained real-window screenshots on Windows, macOS, and Linux remain required. |
| Input | Working with native qualification debt | Python alpha covers keyboard, WASD, mouse, and gamepad paths. Native shell centralizes logical keyboard and any-controller movement, confirm, back, pause, help, replay browsing/playback, settings, and quit; schema-1 bindings capture and persist keyboard buttons, controller buttons, and deliberate controller axes with conflict safety and drift rejection. Xbox, PlayStation, Nintendo, and generic prompt families switch only after deliberate input; vector badges retain text labels. Preferences schema 7 applies a shared bounded stick deadzone while retaining digital D-pad fallback and exposes the Vibe adaptation opt-out and local playtest consent through the same logical keyboard/controller route. A retained nine-case matrix proves exactly-once keyboard, D-pad, and stick turn consumption under low, normal, and stressed render schedules with identical rules hashes. Physical multi-controller and visible-review evidence remain. |
| Achievements | Working | Twenty-five Python profile achievements remain in the reference alpha. Native pure `AchievementCatalog` evaluates run-local candidates; current definitions explicitly require Vibe, so Classic cannot silently advance them. Product Vibe runs emit once-only terminal `AchievementCandidate` events with shell captions. Schema 1 `achievements.json` stores permanent unlock IDs; shell load/save, `ApplyProfileUnlocks`, and full-catalog browse (`U`/LB via `AchievementsBrowseReport`) are live in Godot. The completed 25-row V070-08 audit retains 17 native Vibe-only candidates and documents all eight reference-only exclusions. |
| Progression and cosmetics | V080-04 automated foundation complete; human review pending | Twenty exact goals cover mastery, discovery, and identity across early, middle, and mastery pacing. Eight curated cosmetic sets expose exact requirements, distinct non-color identities, selection, and five unique saved loadouts through keyboard and controller routes. The twelve-event finite Broadcast Tour spans four tiers with fixed seeds, canonical rivals and stations, exact primary/style goals, expression-only rewards, replay recording, immediate rematch, and noncompetitive practice isolation. Schema-1 `progression.json` rejects forged human identity, unearned rewards, invalid dependency order, and locked cosmetic selection. Automated qualification is green with zero reviewed human sessions, so pacing and visual appeal remain human gates. The Python oracle's 10,800 combinations are a frozen reference fact, not the native product catalog. |
| Leaderboard and personal bests | Automated native foundation complete | Native terminal scores enter a bounded top ten per exact rules, mode, purpose, seed, difficulty, DDA, and config category while schema-2 personal bests retain the category record. Keyboard V or Down and controller Down open the Local Scores browser. Confirmed import from `user://imports/high_scores.json` is exact-once, source-preserving, SHA-256 recorded, and visibly noncompetitive under `Legacy 0.2`. The Python oracle retains its own frozen repository. |
| Replays | V080-01 automated foundation complete; human/platform review pending | The background browser shows date, mode, rules, score, seed, duration, and verified/incompatible/modified/unreadable state without internal names or hashes. Only verified entries play or export. Keyboard/controller controls cover 0.5x/1x/2x/4x speed, pause, step, back-ten, HUD, restart, and return. Export is atomic and bounded; exact content-hashed deletion consent cancels losslessly, rejects stale plans, deletes one stored replay, and preserves exports. `replay-browser-qualification-v2` proves progression isolation. Offline ghost and challenge integration is complete under V080-11; retained platform pixels and accessibility review remain. |
| Capture and sharing | V080-08 automated foundation complete; human/platform review pending | The remappable Help action toggles a default-off clean view during gameplay, replay, or spectator playback through keyboard H or controller L3, hiding run HUD, replay controls, terminal, audio status, debug, and spectator overlay families without changing rules state. Explicit verified replay export also writes an atomic, idempotent, no-overwrite 24-field run summary with complete version, rules, mode, configuration, seed, integrity, and outcome metadata and no player identity or private paths. `capture-sharing-qualification-v1` is green. Retained Windows, macOS, and Linux captures and final trailer composition remain. |
| Save durability | Automated foundation complete | Schema-versioned atomic repositories live in OS user-data directories. Data settings separately reset preferences/bindings, progression, local scores and personal bests, replays, and optional content. Exact targets are shown before confirmation; bounded background backup is SHA-256 verified before removal; cancel does not write; quit waits; corrupt/incomplete backups are visible and non-restorable; restore refuses overwrite conflicts and reloads runtime state. Human recovery-language and platform file-browser review remains. |
| Player preferences | Working | F1/controller Start opens 6 sections and 34 described rows. Schema-7 atomic preferences persist default-off local playtest consent, the Vibe adaptation opt-out, independent Master/Music/SFX/UI values and mutes, Master-bus mono downmix, three window modes, four window-size presets, contrast, reduced motion, text scale, shake, flash-free state, and a shared 10 to 90 percent controller deadzone while migrating schemas 1 through 6. Section restore, DDA category isolation, local summary export/deletion, display apply/reload, mono apply/reset/reload, D-pad digital fallback at maximum deadzone, separate tutorial reset, verified preferences/bindings backup and reset, cancel-without-write, reload, and visible session-only save-failure fallback pass raw keyboard/controller smoke. Four-profile accessibility evidence prohibits full-screen flashes, zeros protective-profile shake, retains all cues and critical text, and never shortens captions. |
| AI spectator mode | V080-09 automated foundation complete; human experience review pending | The native Godot circuit lets keyboard and controller users select two of ten measured world-bible rivals, Classic or Vibe rules, twelve seeds across three classes, four speeds, three explanation levels, and four informational predictions. Deterministic lanes share seed and rules while typed overlays show target, risk, survival resources, Vibe Level, record delta, and a concise reason. Fifty authored event lines, ten distinct sheds, station affinities, ten local standings and challenge records, pairwise rivalries, and seven milestone types persist atomically without player identity. Exact seed challenges exclude AI decisions and random state and cannot advance ordinary human progression. Automated recovery covers stalls, invalid unofficial channels, missing commentary, unavailable audio, and repeated switching without rules mutation. `spectator-experience-qualification-v1` is green; physical controller-family, multi-platform presentation, editorial, pacing, and entertainment review remain. |
| Optional lore | V080-10 automated foundation complete; human editorial and pacing review pending | A closed pure C# catalog exposes 41 offline entries at Surface, Discoverable, and Archive depth. Nineteen surface entries cover eight stations, ten rivals, and nine mutations; fourteen discoverable entries span six content kinds; eight archive entries span transcripts, timelines, mysteries, and alternate interpretations. Existing local rewards, spectator milestones, and replay counts unlock optional depth without awarding progression or changing rules. The Godot archive supports raw keyboard and controller filtering, scrolling, locked-state explanations, and return. `optional-lore-qualification-v1` verifies copy IDs, continuity, safety, unlock completeness, offline operation, input, namespace separation, and rules isolation. Human canon, tone, humor, maximum-text-scale platform presentation, and curiosity-pacing review remain. |
| Offline comparisons | V080-11 automated foundation complete; human household and platform review pending | Schema-1 tamper-evident seed codes bind rules, content, mode, configuration, gameplay seed, and three allowed challenge options. Four fixed household slots explicitly copy verified replays under a 16 MiB bound, preserve every source, reject modified or incompatible files, and require fresh content-hashed deletion consent. Equal-rules ghosts run beside the player without entering collision, scoring, random state, or progression. Atomic idempotent 26-field run cards include closed run facts while excluding player identity and private paths. The Godot browser and ghost HUD pass raw keyboard/controller routes. `offline-comparison-qualification-v1` proves the live path, isolation, privacy, recovery ownership, and core-offline operation. Household handoff, maximum-text-scale platform presentation, and ghost readability remain human gates. |
| Radio | Automated native behavior foundation complete; pack approval pending | Packaged station/track metadata comes only from fully validated optional-pack manifests and inventory. Normal native source play also discovers the public eight-station, 95-track checkout library and routes the current MP3 through the Music bus. Playback policy and Godot's one-track adapter define shuffle/no-repeat, single-track repeat, pause/resume, station switching, end-of-track, mute/help state, and missing-track/pack recovery with separate radio RNG. Menu, run, and Content Packs expose bounded state; `J` and controller `R3` cycle stations. The library remains blocked from native export pending loudness, credit, allowlist, physical decode, and listening approval. |
| Broadcast | Automated policy foundation complete; authored content pending | Eight planned SBN station identities each declare inclusion, host, visual, short-ID, transition, and Coil metadata while remaining explicitly unapproved. Host material is limited to run start, major milestone, recovery, and post-run, with a 100-step cooldown and eight-segment run cap. Ordinary combos keep the track continuous, critical gameplay wins interruption priority, captions survive missing audio, and adaptive layers remain disabled without compatible material. Qualification proves no-repeat selection, fatigue bounds, and RNG/rules isolation; rights, loudness, voice, stingers, and listening approval remain pending. |
| Feedback and sound effects | Typed fallback production working; authored production pending | A closed matrix covers all 19 ordered rules events and 15 shell-action families with dominant visual/audio/text/haptic behavior, priorities, cooldowns, bounded polyphony, stack/interruption/ducking policy, and accessibility alternatives. Thirty-one finite native fallbacks give unique identities to navigation, combo tiers/break, restart, achievement, both death causes, and all nine power activations. Every cue is connected, fingerprinted, provenance/license declared, peak measured, and rules-isolated. Native playback fails closed, keeps visual cues active, retries after a bounded delay, repairs missing buses, and records stress evidence. Authored SFX remain explicitly absent; physical-device, haptic, mix, and listening review remain. |
| Visual hierarchy | Automated foundation complete | One production policy caps 160 simultaneous particles, 64 per event, one 0.35-strength shake source, zero full-screen flashes, three popups, one overlay, three protection-first head outlines, and bounded popup text. Peak treatment is reserved for death prevention, death, major achievement or grid completion, and maximum combo. Live captions, outlines, head direction, and terminal opacity consume this policy. Both board palettes pass 3:1 foreground contrast, and five deterministic quiet/busy/warning/recovery/game-over PNG review frames are hash-gated. Retained live platform pixels and subjective hierarchy review remain pending. |
| Performance cost | Automated foundation complete; named hardware pending | Minimum, default, and maximum-safe profiles each capture 40 live Godot frames with average/p50/p95/p99/max statistics. The maximum mixed scene accounts for all 2,112 board cells using 2,107 snake cells, three hazards, food, and a power signal, plus 160 particles and three popups. Production budgets publish 60 FPS, 16.67 ms, 12 audio channels, and at most 2,400 logical draw submissions. Shared CI enforces 25 ms average and 50 ms p95 gross-regression ceilings while retaining p99 and maximum as diagnostics, including on failure. Three identical 256-step rules hashes prove presentation does not change simulation speed. Named minimum Windows/macOS/Linux hardware, driver draw calls, allocation, memory, thermal, and long-session acceptance remain pending. |
| Vibe Level | Automated foundation complete; authored layers and feel pending | One director owns Grounded/Flow/Heat/Overdrive/Transcendent at combo 0/3/5/10/20. Every level declares board, HUD, trail, particle, camera, music, stinger, and static accessibility budgets. Live board tint, combo styling, trails, captions, and stingers consume typed state. Duplicate observations do not refire transitions; combo break returns once to Grounded. Thirteen fixed scenes and seven accessibility profiles retain critical-gameplay priority, at least 4.56:1 contrast, static level identity, unchanged rules hashes, and unchanged score categories. Authored music layers and human recognition/fatigue review remain. |
| Power-ups | V070-09 automated foundation complete; human decision pending | All nine powers are pure C# and product Vibe can offer every kind deterministically. Protection, tempo, harvest, and geometry family rules suppress tactically redundant offers while preserving declared harvest synergies. Godot shows family, stable marker, effect, duration, and held state before collection and beside active powers. Schema-2 local summaries retain only per-power aggregate offer, detour, collection, activation, expiry, consumption, save, and death-adjacency counts. Six seeded synergy scenarios pass; matching human observation and the default-off Mutation Fork keep/remove decision remain pending. |
| Adaptive difficulty | Automated foundation complete | `vibe-bounded-hunger-v1` is pure, deterministic, stateless, and Vibe-only. Support, Standard, and Pressure change only hunger drain within zero to two ticks per step. Live HUD and run-end metadata disclose the state and policy. Classic rejects adaptation; current preference schema 7 retains the unranked Vibe opt-out; DDA-on/off categories and config hashes never compare. `adaptive-fairness-qualification-v1` proves bounds, closed inputs, determinism, replay restoration, settings routes, metadata, achievement eligibility, and category isolation. The observed baseline captures DDA-on/off simulation separately; human fairness and feel remain pending. |
| Configuration | Working | Schema version 1 validates types and ranges; changes still require restart. |
| Player CLI and updates | Working | `vibesnake play`, `update`, `status`, `doctor`, and `version`; `play.ps1` / `play.sh` / `play.bat`; install scripts; GitHub `main` fast-forward reinstall. |
| Packaging | Partial | Source and wheel player-latest artifacts exist. Runtime assets still use source-tree-relative paths; a bare wheel is not a fully self-contained game without assets. |
| Automated gameplay QA | Deterministic laboratory and observed baseline complete | Nine policies cover reference AI, lifecycle stress, input stress, and replay-oracle behavior across three fair-score variants. Ten hostile scenarios, 324 paired runs, 124,242 compared steps, 27 distributions, seven verified outlier replays, and reviewed fixed/exploratory/failure corpora pass. A separate 100-seed matrix retains 2,700 per-run metric records and 27 hash-locked observed distributions for score, survival, length, food, combo, powers, deaths, and outcomes. These are AI observations, not human targets. |
| Content inventory | Foundation working | Deterministic policy and generated inventory cover 114 public assets including the radio library. Export eligibility is deliberately zero until pack quality gates pass. `ContentEligibilityReport` and `content-eligibility-evidence-v1` JSON summarize ship/rights/media breakdowns for pack-approval handoffs. |
| Content curation | V080-05 automated handoff complete; human selection pending | `vibesnake-content-curation-v1` accounts for all 95 radio tracks exactly once across eight balanced 11-to-13-track station queues. Rights and structural MPEG integrity are complete; duplicate radio bytes and suspicious temporary/test filenames are zero. `content-credits-v1` renders deterministic manifest-bound credits and third-party notices without timestamps or machine paths. Approved tracks, authored core-music candidates, retained full-decode/loudness/listening evidence, production manifests, and export-eligible files all remain zero. |
| Creator content | V080-06 automated foundation complete | Native `ValidateCreatorContent` commands validate a custom personality or an exact canonical core-plus-radio pack set and emit stable schema-1 JSON. Two closed schemas, two examples, sixteen personality codes, fifteen pack/compatibility codes, and no-override collision rules are published. Core resolves first; unique optional IDs resolve ordinally; duplicate IDs fail. Qualification prohibits process, dynamic/native/reflection load, scripting, network, and engine references and locks `executesContent: false` plus `arbitraryCodeSupported: false`. Production manifests remain unavailable until V080-05 approval. |
| Localization | V080-07 automated foundation complete; human platform review pending | `ShellLocalization` owns 509 stable English IDs and 67 exact named-parameter templates across thirteen shell flows, runtime statuses, typed step feedback, stable broadcast captions, all 41 lore entries, and offline comparison copy. Opt-in `qps-ploc` expansion is deterministic, preserves glyph parameters, measures at least 1.3125 expansion, finds zero missing fallback-font glyphs, and fits at 150 percent text. Evidence resolves 18 onboarding, 24 feedback, and 24 broadcast IDs and requires zero direct draw, prompt, static status, composed status, or audited domain-presentation expressions. Visible Windows, macOS, and Linux review remains. |
| Content pack contract | Native contract working; production approval pending | Pure C# schema 1 validates bounded canonical core and radio manifests against exact inventory, metadata, rights, credit, dependency, compatibility, and station allowlists. Installed optional packs receive exact allowlist, size, and hash validation; explicit removal moves only the selected pack into recoverable quarantine; restore revalidates it; player data stays separate. Godot isolates absent, removed, malformed, incompatible, tampered, and duplicate optional packs and retains core-only offline evidence. No production manifest is export-approved yet. |
| Target technology | Qualification in progress | Godot 4.7.1 and .NET 10.0.302 pinned. Pure C# kernel covers core rules and all nine powers. Godot shell honors full-power presentation and tempo cadence. Hosted multi-platform player smoke exists. Pack export, deeper parity fixtures, and feel review remain open. |

## Inventory facts

- Radio library: 95 original MP3 tracks assigned exactly once across eight stations in public source under `assets/audio/radio/`.
- Public inventory: 114 files, 340,378,770 bytes, all rights-cleared, structurally valid; 106 blocked for pack export, 8 excluded development references; 0 export-eligible.
- Achievements: 25 definitions across common, rare, epic, and legendary tiers.
- AI: 10 compatibility personalities in both the frozen oracle and pure C# product catalog; one Python-oracle custom example. Strict native custom validation is not complete.
- Cosmetics: 12 colors, 6 patterns, 5 eye styles, 6 accessories, and 5 trails.
- Game flow: 12 enumerated states managed by an explicit transition map.

An optional ignored local `archive/` may preserve historical production records on developer machines. It is not part of the public repository or release authority.

## Improvements completed since the prior audit

- Pure C# rules kernel + Godot shell advanced as the **primary product surface** (nine powers, multi-power synergy campaigns, achievements profile + browse, schema 3 session counters, property campaign evidence, content eligibility reports, architecture purity bans).
- Dual-runtime achievement-candidate product-path fixture proves flag-on parity without flipping default-off corpora.
- Hosted CI exercises native rules and native player smoke on Windows, macOS, and Linux without Python.
- Single public `main` branch with green CI; Dependabot version-update spam disabled.
- Reference path: eight-station offline radio in public source, adaptive Python presentation, `player-latest` packages, player CLI.

Earlier 0.2.0 foundations remain in effect; see [CHANGELOG.md](../../CHANGELOG.md).

## Release blockers

1. **Godot shell depth:** physical multi-controller/audio-device evidence, retained platform scaling screenshots, authored cues, and feel review. Central theme/vector prompt qualification and automated fallback stress/recovery are complete.
2. **Protected platform signing, selected-store integration, and player-facing pack recovery (V030-10)** while exact deterministic qualification archives, store-depot shapes, checksums, portable export, manifest, read-only install, consent-bound recoverable quarantine/restore, and player-data separation gates already pass.
3. **First export-eligible core/radio packs** (`exportEligible` still zero): loudness, credits, allowlists.
4. Expand native artifact smokes through physical controller/device-change and retained screenshot paths; automated audio-graph failure, scaling math, and lifecycle paths now retain evidence.
5. Replace remaining repository-relative asset assumptions with the target content service for ship packs.
6. C# QA depth: permanent corpus compaction and broader campaigns (bounded replay metadata/status browse, playback, export/delete, offline ghosts/challenges, and all powers already pure C#).
7. Structured playtests for controls, difficulty, power choices, readability, audio fatigue, accessibility, restart desire.
8. Complete the retained named-hardware, physical audio, authored broadcast, accessibility, and human feel gates for the automated performance, Vibe Level, radio, broadcast, event-to-SFX, multimodal, and visual-hierarchy foundations.

**Do not treat Python feature work as progress toward 1.0.** Keep the oracle green; ship in Godot/C#.

The recommended implementation order is in the [roadmap](../../ROADMAP.md) (**Product path** section).

Current roadmap milestone: **0.3.0** technology qualification and native vertical slice. Later milestones stay gated behind clean native artifacts so new behavior is built in the architecture players receive.
