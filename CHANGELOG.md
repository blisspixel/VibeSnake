# Changelog

Notable player-facing and engineering changes are recorded here. The project is pre-release and does not yet promise semantic-version stability.

## Unreleased

- Completed the eighteenth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns `core-rules`, `core-rules-write`, and combined freshness validation for the 35 reviewed Python-origin Core Rules vectors. The closed renderer preserves `python-core-reference-v3`, exact configuration, case, state, and ordered-event semantics, and the checked fixture's 57,031 UTF-8 bytes with SHA-256 `a3ca486b827221a36fd86b97d62b5ddc9a409576a471f7793779e1b9e59f38af` without executing `SnakeRun`, rules helpers, scoring, or randomness. `SharedRuleTraceParityTests` remains the independent live consumer and closes provenance, scopes, ordered IDs, queue acceptance, score, combo, speed, length, saturation, wrap, starvation, collision precedence, normalized random use, and full-grid victory. CI and pre-commit use the native route, and the superseded Python generator and its four tests are removed. The six closed native renderers now preserve 67 reviewed vectors as exact canonical LF bytes, and the shared fixed-fixture lifecycle has positive render and write/read coverage at its inclusive 65,536-byte limit. The focused repository-check surface now has 277 contracts. The measured suites pass 612 Python tests plus 14 subtests at 86.95 percent line coverage and 1,539 native contracts with zero build warnings. `CoreRulesFixtureCheck` measures 547 of 554 lines and 43 of 44 branches, or 98.73/97.72 percent line/branch coverage. RepositoryChecks measures 94.56/86.06 percent line/branch, and aggregate native coverage measures 94.28/86.73 percent. Movement fixture generation is next under a separately designed bounded large-file lifecycle because its 999,087-byte corpus exceeds the fixed-fixture limit. Player behavior, fixture bytes, and public gameplay schemas are unchanged.

- Completed the seventeenth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns `remaining-powers`, `remaining-powers-write`, and combined freshness validation for the nine reviewed Python-origin Slow-Mo, Boost, Magnet, Bait, Gluttony, and Segment Detach vectors. The closed renderer preserves `python-production-remaining-powers-v1`, exact case, state, and event order, and the checked fixture's 9,548 UTF-8 bytes with SHA-256 `92bec4caeca22c28d81ced7045168e93aeeb946df71e8c9a61872798210ba2b9` without executing `SnakeRun`; `SharedRemainingPowerTraceParityTests` remains the independent live consumer and closes provenance, randomness, scopes, exact config, case order, uniqueness, pickup identity, and food-skip identity. CI and pre-commit use the native route, and the superseded Python generator and its two tests are removed. The five native renderers now preserve 32 reviewed Python-origin vectors as exact canonical LF bytes. `RemainingPowersFixtureCheck` measures 367 of 374 lines and 21 of 22 branches, or 98.12/95.45 percent line/branch coverage. The focused repository-check surface now has 270 contracts. The measured suites pass 616 Python tests plus 14 subtests at 87.27 percent line coverage and 1,532 native contracts with zero build warnings. RepositoryChecks measures 94.35/85.92 percent line/branch, and aggregate native coverage measures 94.20/86.69 percent. Core Rules fixture generation is next because its 35 reviewed cases form the smallest remaining independent corpus at 57,031 bytes and still fit the shared bounded fixture lifecycle. Player behavior, fixture bytes, and public gameplay schemas are unchanged.

- Hardened the native CI coverage lane after a degraded hosted Windows runner consumed the former 20-minute job budget without returning test output. The native rules matrix now has 30 minutes so its documented clean coverage retry can fit, and the shared coverage wrapper streams each test line while retaining only bounded strict-pass and Coverlet-truncation flags. Contract checks pin the timeout, live output, constant-memory classification, and retry protections. Test semantics, coverage floors, retry eligibility, and player behavior are unchanged.

- Completed the sixteenth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns `shield`, `shield-write`, and combined freshness validation for the eight reviewed Python-origin Shield vectors. The closed renderer preserves `python-production-shield-v1`, exact case and event order, and the checked fixture's 4,489 UTF-8 bytes with SHA-256 `86378f4d69dfbb3fc8929377c091ba32ebea59b03e8f55e1ba9a087d3a0d3e8e` without executing `SnakeRun`; `SharedShieldTraceParityTests` remains the independent live consumer and now closes provenance, randomness, scopes, exact config, case order, uniqueness, and pickup identity. CI and pre-commit use the native route, and the superseded Python generator and its three tests are removed. Six focused Shield fixture contracts pass with the renderer at 98.13/95.83 percent line/branch coverage, and the focused repository-check surface now has 263 contracts. The measured local suites pass 618 Python tests plus 14 subtests at 87.47 percent line coverage and 1,525 native contracts with zero build warnings. RepositoryChecks measures 94.19/85.86 percent line/branch and aggregate native coverage measures 94.15/86.67 percent. Remaining Powers fixture generation is next because its nine reviewed cases form the smallest remaining independent corpus at 9,548 bytes. Player behavior, fixture bytes, and public gameplay schemas are unchanged.

- Completed the fifteenth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns `phase-shift`, `phase-shift-write`, and combined freshness validation for the six reviewed Python-origin Phase Shift vectors. The closed renderer preserves `python-production-phase-shift-v1`, exact case and event order, and the checked fixture's 3,534 UTF-8 bytes with SHA-256 `1ebe90414c4556d5e6708792822771772c14f041d54fd0d8095feadbc4affd32` without executing `SnakeRun`; `SharedPhaseShiftTraceParityTests` remains the independent live consumer and now closes provenance, randomness, scopes, exact config, case order, uniqueness, and pickup identity. The shared fixed-fixture layer gains positive regressions at its exact 256-entry root and destination-parent capacity boundary. CI and pre-commit use the native route, and the superseded Python generator and its three tests are removed. Six focused Phase Shift fixture contracts pass with the renderer at 97.89/95.83 percent line/branch coverage, and the focused repository-check surface now has 256 contracts. The measured local suites pass 621 Python tests plus 14 subtests at 87.62 percent line coverage and 1,518 native contracts with zero build warnings. RepositoryChecks measures 94.04/85.78 percent line/branch and aggregate native coverage measures 94.10/86.64 percent. Shield fixture generation is next because its eight injected-state cases already have an independent native consumer and form the next-smallest deterministic corpus. Player behavior, fixture bytes, and public gameplay schemas are unchanged.

- Completed the fourteenth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns `last-stand`, `last-stand-write`, and combined freshness validation for the five reviewed Python-origin Last Stand vectors. The closed renderer preserves `python-production-last-stand-v1`, exact case and event order, and the checked fixture's 3,596 UTF-8 bytes with SHA-256 `3c2cfec6ae10d7a3b0ba3e1dd753ee077d2297224a3f098c1c74addbd706e596` without executing `SnakeRun`; `SharedLastStandTraceParityTests` remains the independent live consumer and now closes provenance, randomness, scopes, config, case order, uniqueness, and pickup identity. Achievement candidates and Last Stand now share a narrow fixed-fixture file layer that bounds generated and checked bytes, verifies stable reads, rejects symbolic/reparse-point paths and portable case aliases, revalidates the fixed path immediately before flushed same-directory replacement, preserves the primary failure if cleanup also fails, truncates diagnostics without splitting a surrogate pair, and self-verifies exact output. CI and pre-commit use the native route, and the superseded Python generator and its two tests are removed. Eight focused Last Stand fixture contracts pass with the renderer at 97.89/95.45 percent line/branch coverage, and the focused repository-check surface now has 249 contracts. The measured local suites pass 624 Python tests plus 14 subtests at 87.78 percent line coverage and 1,511 native contracts with zero build warnings. RepositoryChecks measures 93.89/85.69 percent line/branch and aggregate native coverage measures 94.06/86.62 percent. Phase Shift fixture generation is next because its six injected-state cases already have an independent native consumer and form the smallest remaining deterministic corpus. Player behavior, fixture bytes, and public gameplay schemas are unchanged.

- Completed the thirteenth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns `achievement-candidates`, `achievement-candidates-write`, and combined freshness validation for the four reviewed Python-origin achievement vectors. The closed renderer deliberately does not execute `SnakeRun`, so `SharedAchievementCandidateTraceParityTests` remains an independent live C# consumer rather than comparing native rules with native-generated expectations. It reproduces the checked fixture's exact 2,682 UTF-8 bytes and SHA-256 `262701784c6736b04ff1d97d641d606bc1ac5de0d6cb51494c6476aca1b7fcd6`, preserves `python-core-reference-v3` provenance, bounds reads and sibling discovery, rejects stale bytes, directories, links, and portable case aliases, and atomically replaces only the fixed fixture with cleanup and exact self-verification. The parity consumer now closes provenance, randomness, scope, config, case order, and empty-command fields. CI and pre-commit use the native route, and the superseded Python generator and its one test are removed. The remaining Python fixture hooks now prepend the checkout's `src` tree, preventing an older installed package copy from producing false freshness failures. Eight focused fixture contracts pass at 96.21/90.74 percent line/branch coverage, and the focused repository-check surface now has 240 contracts. The measured local suites pass 626 Python tests plus 14 subtests at 87.91 percent line coverage and 1,502 native contracts with zero build warnings. RepositoryChecks measures 93.70/85.78 percent line/branch and aggregate native coverage measures 93.99/86.65 percent. Last Stand fixture generation is next, followed by Phase Shift. Player behavior, fixture bytes, and public gameplay schemas are unchanged.

- Completed the twelfth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns stable-promotion qualification through `stable`, `stable-write`, and `stable-record`, and the combined route validates its foundation on every hosted operating system. A new `vibesnake-stable-upstream-acceptance-v1` authority closes ten decision IDs, ten exact accepted kinds, 43 ordered external gates, generic and special decision schemas, and the unsigned-to-signed artifact boundary. Nine reviewed decisions bind one unsigned candidate cohort; protected platform signing must bind those inputs and the byte-changing final public artifacts, manifests, and provenance. The validator rejects generic favorable JSON, reused decisions or evidence, unsafe or case-colliding paths, incomplete retained closure, authority drift, malformed manifests and checksums, public-install mismatch, optional-pack mismatch, and any weakening of the six stable compatibility acknowledgements. It writes canonical LF `stable-promotion-handoff-v2` evidence atomically, with protected execution and release acceptance false until an exact retained record passes. It never performs review, hardware execution, signing, tagging, installation, upload, withdrawal, or publication. CI and pre-commit use the native authority, and the superseded Python validator and its four tests are removed. Twenty-six focused stable contracts pass at 97.93/85.30 percent line/branch coverage. The measured local suites pass 627 Python tests plus 14 subtests and 1,493 native contracts with zero build warnings, RepositoryChecks at 93.58/85.70 percent line/branch coverage, and aggregate native coverage at 93.97/86.63 percent. Achievement-candidate fixture generation is next because its four fixed cases already have a native consumer and establish the smallest safe canonical regeneration pattern. Player behavior and public gameplay schemas are unchanged.

- Hardened native external-process cleanup after hosted coverage exposed two timing edges. Screenshot capture and dependency resolution now share one bounded runner that limits captured output, attempts best-effort process-tree termination, bounds termination and output draining, distinguishes exact-cap output from true truncation, and fails closed when a descendant retains an output pipe after its parent exits. The Windows dependency contract uses the lightweight system command processor rather than measuring PowerShell startup under full-suite load, and host-dependent process contracts run outside parallel simulation campaigns. The changes preserve successful capture and lock output. The four pinned Godot 4.7.1 screenshots were recaptured, revalidated, and visibly reviewed after the screenshot source fingerprint changed.

- Completed the eleventh bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns the V090-10 rehearsal foundation and retained staged-record validation through `rehearsal`, `rehearsal-write`, and `rehearsal-record`. A new checked-in `release-materials-acceptance-v1` authority defines the only accepting transition from structural materials: exact revision, version, candidate, three artifact manifests, the retained structural handoff, and separate passing artifact-reconciliation, claim-approval, visible-image, and video-playback gates. The rehearsal authority requires strict duplicate-free bounded UTF-8 JSON, canonical semantic versions and UTC times, portable NFC paths, link-free containment, exact record and retained-file closure, schema-3 Release manifest identity, stable streaming hashes with a final snapshot recheck, 33 passing platform-operation attestations, unchanged protected-data digests, complete withdrawal, four verified non-personal operational roles, nested-evidence and output-alias protection, bounded diagnostics, canonical LF `release-rehearsal-handoff-v2` evidence, and atomic replacement. V2 carries the exact source revision, version, record identity, candidate artifact map, and manifest map required downstream. Automated validation does not perform platform operations or human approvals, and the checked-in foundation keeps execution, completion, and release acceptance false. CI and pre-commit use the native authority, the combined route covers the foundation on Windows, macOS, and Linux, and the superseded Python validator and its four tests are removed. Stable promotion is next because its current generic favorable-JSON check cannot safely consume the real upstream authorities. The measured local suites pass 631 Python tests plus 14 subtests and 1,465 native contracts with zero build warnings, RepositoryChecks at 92.85/85.67 percent line/branch coverage, and aggregate native coverage at 93.80/86.67 percent. Player behavior and public gameplay schemas are unchanged.

- Completed the tenth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns the V090-09 release-material foundation and exact candidate record through the read-only `materials`, evidence-writing `materials-write`, and candidate-bound `materials-candidate` routes. The native authority requires strict duplicate-free UTF-8 JSON, closed contract and candidate fields, bounded regular files, link-free containment, exact document and retained-file hashes, canonical application and revision identity, declared platform/input/size/save-location fields, bounded complete PNG, JPEG table/component/scan, MP4 track/sample/extent, and WebM video-track/keyframe structure, one-read stable retained snapshots and hashes, complete evidence-linked claim identities, portable path and output-alias protection, bounded diagnostics, canonical LF `release-materials-handoff-v2` output, and atomic replacement. A structurally complete candidate sets `candidateMaterialComplete: true` while retaining `releaseAcceptance: false` and the four artifact-reconciliation, claim-approval, visible-review, and playback gates. It deliberately does not inspect artifact manifests, reconcile their contents with declared sizes, approve marketing claims, review screenshots, or play videos; V090-09 remains open until the exact candidate disclosures, media, support route, human review, and playback approval exist. CI retains the pending foundation handoff at its established path, the combined native route validates the foundation on Windows, macOS, and Linux, pre-commit uses the native authority, and the superseded Python validator and its four tests are removed. Release rehearsal is the next dependent validator, followed by stable promotion; the Release matrix remains the earlier candidate producer. The full measured native suite passes 1,449 contracts with zero build warnings, RepositoryChecks at 92.27/85.31 percent line/branch coverage, and aggregate native coverage at 93.71/86.62 percent. Player behavior and public gameplay schemas are unchanged.

- Replaced the unexecuted manual product-matrix session contract with schema 2 before collecting physical evidence. The former validator combined platform-flow passes independently from globally observed device names and profiles, so a device marked `complete-required-flow` could appear in one session without proving its required flow coverage. Each result now binds one flow to one device, any mouse capability, active settings profiles, exact candidate identity, and retained evidence. Only passing observations earn coverage. Release acceptance requires all 144 platform-flow cells, 432 keyboard/Xbox-layout/PlayStation-layout device-flow cells, 16 mouse-capability cells, and 32 platform-profile cells; failed or blocked observations remain fatal. Focused tests prove complete acceptance and reject one-device-per-platform, missing device-flow, missing mouse-capability, failed-profile, malformed dimension, candidate-mismatch, and unsafe evidence cases. The superseded `e87db6e` zero-session workspace remains historical automated evidence only. Release documentation now orders content approval before a fresh candidate, physical execution, repair, and alpha publication. The full local gates pass with 639 Python tests plus 14 subtests, 1,419 native contracts, zero build warnings, 93.87/87.13 percent aggregate native line/branch coverage, and a successful real Godot scene smoke.

- Completed the ninth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns README screenshot capture, exact freshness, and committed-evidence validation while Godot remains the renderer. The read-only `screenshots` route joins the cross-platform combined gate and requires a closed canonical LF manifest, strict duplicate-free UTF-8 JSON, exactly four 1280 by 720 regular PNG files, complete PNG container and decoded-scanline integrity, exact lowercase SHA-256 identities, README references, and a bounded link-free presentation-source fingerprint with normalized text line endings and byte-exact PNG inputs. The explicit `screenshots-write` route builds the game, captures with the exact pinned Godot identity into separate temporary output and player-data directories, validates the complete staged set before replacement, writes the manifest last, re-verifies committed evidence, and states that visible review is still required. Thirteen focused contracts cover exact evidence, stale and duplicate manifests, canonical form, closed directory shape, README drift, corrupt PNGs, metadata and schema drift, line-ending normalization, binary hashing, staged capture, intermediate link containment, pinned executable identity, bounded traversal, bounded output draining when descendants retain pipes, process failures and timeouts, missing capture markers, missing executables, dimension and hash drift, extra output, and command shape. The superseded Python tool and its four-row CI invocation are removed. The four current native captures were regenerated through the pinned Godot 4.7.1 renderer and visibly reviewed without finding clipping, missing text, stale state, or framing defects. The full measured native suite now passes 1,432 contracts with zero build warnings, RepositoryChecks at 92.12/86.09 percent line/branch coverage, and aggregate native coverage at 93.78/86.91 percent. Player behavior and public gameplay schemas are unchanged.

- Fixed every shell prompt that advertised a control as `[Unbound]` while the key worked. The bindings document carries only the four directions plus confirm, back, pause, and restore defaults, so `ResolveActionPrompt` looked each prompt up there and returned `unbound` for `replay`, `help`, `browse_achievements`, and `browse_content_packs`, which that document has never carried. Those four appear on the main menu (Broadcast Tour, cosmetic sets), the cosmetics loadout, the local-scores import, both replay and Agent Arena story playback screens (restart, toggle HUD, turning-point paging), and the Agent Exhibitions prompt row, where playtest round 7 finally read one: `[Unbound] challenge exact seed` for a same-seed challenge that has always been bound to R and controller North. `GameActions` now publishes the fixed prompt tokens for the actions the shell binds itself, prompt resolution falls back to them, and an action neither source knows still reports Unbound because that is then the honest answer. The documented action contract in the input design did not change; only the on-screen prompts stopped contradicting it. A new localization-smoke gate scans every `DrawActionPromptSegment` call in the shell and fails when one names an action nothing binds, retaining `promptActionCount` and `unboundPromptActionCount` as evidence; it was proven to fail by removing one entry and to pass with it restored.

- Corrected the Agent Play MCP-host inventory sentence, which still pinned rules resource v18 after the interoperability table, changelog, baseline, and live `vibesnake://agent/rules` all moved to v19. The previous revision's version sweep matched `rules resource v18` and `rules-resource-v18` and missed the prose form `are v18, v3, v3, and v4 respectively`, so one guide published two different numbers for one contract.

- Closed playtest round 6's remaining protocol finding by moving every argument-shaped decision into the call-tool filter that already owned argument names and JSON types. An unknown `lessonId` used to reach `start_lesson`, throw, and print `"start_lesson" threw an unhandled exception` with a stack trace to host stderr while the client received a recoverable sentence, so an operator reading logs saw a crash where the protocol saw a correction. The filter now also decides closed vocabularies for `lessonId`, `modeId`, `seedVisibility`, `styleContractId`, `rivalPersonalityId`, `actionProfile`, `action`, `initialAction`, and `declaredIntent`, and the published shape of `matchHandle`, `idempotencyKey`, `expectedStateHash`, `gameplaySeed`, `expectedTick`, and both `maximumSteps` bounds. Every vocabulary is read from the live lesson, mode, style, rival, profile, and enum catalogs, so the rejection path cannot drift from the tools. A rejection names the accepted vocabulary or range, the resource that publishes it, and a bounded echo of the value that arrived, and a tool with no optional arguments now says `Optional: none` instead of trailing off. No previously accepted call becomes a rejection. What still throws inside a tool needs match or catalog state the filter cannot see: an unknown or expired handle, a style contract the selected mode does not support, a blind match given a caller seed, a passport whose catalog identity or profile disagrees, and a registry at capacity. Agent Host advances to `0.18.0`, rules resource to v19, and the Agent Plugin to `0.18.0` for the matching skill sentence; the seventeen tools, eight resources, and every gameplay, observation, action, result, lesson, style, receipt, viewer frame, replay, passport, story, and qualification schema are unchanged. The full native suite is now **1,419** contracts.

- The Agent Exhibitions browser now says what Confirm does. Playtest round 6 opened the cabinet that round 5 could not reach and found the next gap immediately: rows read `WATCH AVAILABLE`, which is a state, not an offer, so AA-09b's recorded-first montage was discoverable only by reading the integration guide. The selected row now reads `CONFIRM PLAYS THE RECORDED STORY`, unselected rows keep the shorter availability state, and the screen gained the bound-key prompt row the qualification and passport screens already had, naming the recorded story, the same-seed challenge, and the way back to the menu. The copy catalog is now 734 entries; all of them still fit the logical canvas at 150 percent text under pseudo-localization.

- Completed the eighth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns strict schema-1 content-policy parsing, deterministic inventory generation, exact freshness, release blockers, and atomic replacement for the complete 114-file, 342,510,815-byte public asset tree. The implementation uses bounded link-free traversal, strict duplicate-free UTF-8 JSON, streaming SHA-256, stable classification and duplicate relationships, closed media types, complete PNG container and decoded-scanline validation, WAV and MPEG structure checks, canonical LF output, and fixed release routes. Independent zlib-header and Adler-32 verification fixes a subtle integrity defect where a truncated PNG checksum could still be accepted after the decoder produced every expected scanline. Eighteen focused contracts and extensive corrupt-media variants cover exact current-repository reproduction, repeatable writes, stale state, release readiness, ambiguous and unsafe policy, tree and byte exhaustion, links, malformed JSON and text, and corrupt PNG, WAV, and MPEG inputs. CI and pre-commit now use the native authority, tagged-alpha assembly runs its fail-closed release route, the superseded Python command is removed, and the retained Python pack-tool parity writer now replaces files atomically. The full native suite is now **1,418** contracts with RepositoryChecks at 92.46/87.16 percent line/branch coverage and aggregate native coverage at 93.84/87.11 percent. Player behavior, scored rules, public schemas, and current export eligibility are unchanged.

- Completed the seventh bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns deterministic generation and exact-byte freshness for all eight 300 by 300 station badges through closed station definitions, project-owned 5x7 glyphs, integer-only drawing, a canonical RGB PNG encoder, bounded link-safe paths, and atomic replacement. The new gate fixes the old decoded-RGB comparison, which could accept a PNG with different alpha, metadata, or encoding despite the documented byte-for-byte contract. Eleven focused badge contracts cover exact snapshots, independently decoded canonical PNG structure, valid metadata-only tampering, missing, changed, oversized, unexpected, linked, and non-file inputs, repeatable generation, unsupported definitions, and command behavior. The superseded Pillow generator and its one-use Python helper are removed, the duplicate Python CI step is gone, the content inventory and curation identity are refreshed, and all eight badges retain their intended visual design. The full native suite is now **1,400** contracts with RepositoryChecks at 92.97/86.94 percent line/branch coverage and aggregate native coverage at 93.97/87.08 percent. Player behavior, scored rules, and public schemas are unchanged.

- Completed the sixth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now validates source and packaged Agent Plugins with required canonical manifest metadata, a closed minimal-skill profile, strict duplicate-free JSON, bounded UTF-8 inputs and tree size, link and launch containment, exact packaged components, and streaming SHA-256 verification. Fifty-seven focused contracts cover valid source and 47-file packages, the formerly accepted missing `version` and `description` defect, malformed manifests and frontmatter, unsafe stdio declarations, exact host arguments, tampering, missing files, invalid paths, command behavior, and links. The package script and all CI paths use the native gate; three native runners no longer install the plugin validator's Python dependencies, and the superseded Python validator and eleven tests are removed. The validation-surface change advances only the Agent Plugin patch contract to `0.17.1`; Agent Host `0.17.0` and every gameplay, observation, replay, viewer, and resource schema remain unchanged. The full native suite is now **1,388** contracts with RepositoryChecks at 93.56/87.02 percent line/branch coverage and aggregate native coverage at 94.04/87.09 percent.

- Isolated the host-dependent rules-throughput contract from every other xUnit collection. Independent simulation campaigns may still use all available cores, but they can no longer overlap the 750-step-per-second measurement and turn shared-runner CPU contention into a false regression.

- Last Stand HUD and offer copy now distinguish its automatically triggered save from the three-second recovery the player still steers. Power-decision qualification now uses an odd-by-odd board so geodesic preference is not exhausted by even-grid antipodes.

- Product Vibe power spawns now prefer cells off the wrap-Manhattan food geodesic from the reserved next head, so walking the shortest food route cannot collect the offer by accident. The flag defaults off and is omitted from canonical config like the offer-policy gate, the `vibe@1` factory enables it, and a tight board falls back to ordinary occupancy. Compatibility Shield-only occupancy, Classic, and shared fixtures stay on the default-off path. Replay verification charges the extra occupancy pass when the flag is on. Signal School `power-route` now allows 440 steps so a detour after the 300-tick spawn still fits; Vibe lesson locks and host qualification measurements were regenerated. The observed-balance baseline hash moved with Vibe only; Classic distributions are unchanged. Native contracts are now **1,331**.

- Hunger, protection, and power-status timers now convert remaining rules ticks through the live cadence interval, so Boost halves displayed seconds and Slow-Mo doubles them while rules ticks stay unchanged. Combined Slow-Mo plus Boost still shows base-cadence seconds. The readable run-HUD gate walks Boost, base, and Slow-Mo intervals, and power-decision qualification asserts the halved and doubled labels.

- Independent AI league, observed-balance baseline, and balance-laboratory simulations now run on a bounded worker set equal to the host processor count and reduce results in input order. One SnakeRun remains single-threaded. Live player steps, ghost lanes, and accumulating reliability hashes stay sequential. Architecture and quality standards record that extra cores are for independent work, not for splitting a scored run.

- Completed the fifth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now verifies the committed 1024 by 1024 brand logo by PNG signature, IHDR dimensions, and the approved SHA-256 identity, using POSIX-relative diagnostics and no image library. Three new contracts cover missing and directory paths, truncated and non-PNG headers, wrong IHDR type, dimension order, body and trailing-byte hash drift, current-checkout success, command isolation, and combined-route failure. CI and pre-commit run the native `logo` command; the superseded Python verifier was removed. The combined native check is now **1,309** contracts. Player and asset bytes are unchanged.

- Completed the fourth bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now validates and regenerates both Python dependency-lock profiles with ordered path-plus-byte input identities, strict native headers and LF output, exact pins, SHA-256 hashes, exact `uv 0.11.33`, fatal resolver warnings, bounded version and resolution calls, checkout-resolver preference, and atomic same-filesystem replacement. The deterministic refresh advances Ruff from 0.16.3 to 0.16.4 within the existing constraint and leaves the player runtime graph unchanged. Seventeen new contracts cover both current graphs, containment, input order and byte drift, malformed locks, strict UTF-8, resolver versions, failures, warnings, timeouts, generated output, process execution, command isolation, and temporary-file cleanup. The process-capture contract gives hosted Windows Coverlet runs a 30-second PowerShell start budget while still proving a 100-millisecond hang is killed. The combined native check runs on Windows, macOS, and Linux. The superseded Python command, module, and six tests were removed.

- Stabilized the focused bare-loop pacing gate after unchanged `macos-latest` runs recorded 64.27 ms, 60.80 ms, and 62.11 ms p95 results while an independent run of the same revision passed at 48.43 ms. An initial focused-envelope miss now captures three identical 40-frame bursts and reduces them by the per-frame minimum before applying the unchanged 25 ms average, 60 ms p95, and 100 ms maximum gates. Shared-runner scheduling can add delay but cannot make a frame faster, so independent stalls are removed while a rendering regression present in every replicate remains fatal. Evidence retains every raw burst summary, the reduction policy, and the final summary. The separate three-profile 25 ms sustained-average and 70 ms p95 regression gate is unchanged.

- Completed the third bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now validates the exact candidate-freeze policy, resolves 93 frozen-surface files with overlapping contract ownership and generated-tree exclusion, verifies active baselines and current file hashes, and prepares deterministic baselines through an atomic native command. Thirteen new contracts cover malformed and duplicate JSON, invalid UTF-8, unsafe and empty patterns, ordered policy fields, prerequisite and severity drift, valid UTC and revision identity, deterministic file ordering and combined hashing, manifest tampering, source drift, frozen activation, command isolation, and temporary-file cleanup. The combined native check runs this policy on Windows, macOS, and Linux. The superseded Python command and seven tests were removed.

- Completed the second bounded V030-13 validator-retirement slice. Native `RepositoryChecks` now owns active authored-file discovery, excluded-tree handling, credential and signing-material rejection, unfinished markers, prohibited Unicode, attribution policy, strict UTF-8, and AST-equivalent Python placeholder checks without starting Python. Ruff remains the complete Python syntax parser while the frozen oracle exists. Fourteen new contracts cover scope, every Unicode range, credentials across the checkout, ignored generated and historical trees, strings and comments, inline and multiline placeholders, malformed strings and delimiters, deduplication, command isolation, and deterministic diagnostics. The combined check runs on Windows, macOS, and Linux. The superseded Python command, module, and five tests were removed.

- Completed the first bounded V030-13 validator-retirement slice. The native `RepositoryChecks` command now owns canonical documentation discovery, local-link validation, changelog contract-release uniqueness, and exact product-version alignment. Thirty-five focused contracts cover canonical and malformed SemVer, strict UTF-8 and LF handling, missing and duplicate declarations, cross-file drift, encoded and root-relative links, fenced examples, missing targets, invalid paths, duplicate changelog claims, command isolation, and deterministic diagnostics. CI runs the combined command on Windows, macOS, and Linux, and the tagged-alpha assembly path uses the native version check after a locked restore. The two superseded Python command entry points were removed; the shared Python version helper remains only where later release-assembly migration slices still require it.

- Bound manual product review to exact Release evidence. A new preparer independently recomputes the three-platform Release matrix, projects its package and manifest hashes into all four physical platform rows, and creates intentionally incomplete session templates. The validator now rejects sessions without that candidate record or with a mismatched revision, version, or artifact hash. The current `e87db6e` handoff is qualified with zero of 144 cells executed and release acceptance false.

- Added a hash-bound radio listening handoff for lossless station review copies. It rehashes every FLAC before preparing or validating a record, requires full-playback, distortion, edge, relative-level, station-fit, and sustained-comfort decisions for every track on headphones and speakers, and preserves honest rejection and blocked results. The current 12-track `the_bureau` handoff is technically verified while listening, source replacement, release approval, and export eligibility remain pending or false.

- Fixed the Release-only native export failure exposed by the first clean manual three-platform dispatch. `ExportRelease` correctly removes the Agent Arena assemblies, but localization qualification still referenced one preview-only survival constant in four places. The layout gate now derives the same three non-reversal candidates from the four-direction rules catalog, asserts parity when the preview is compiled, and ordinary native qualification compiles `ExportRelease` so this defect cannot hide behind green Debug exports. Exact hosted Release run 32421705560 passes at `e87db6e` and retains Windows x64, macOS Universal, and Linux x64 packages, the cross-bound matrix, qualification evidence, and detached provenance. The aggregate records 300 clean launches, 600,000 reliability comparisons, 300 spectator restarts, 21 injected faults, 360 performance samples, and 24 maximum-text-scale display rows.

- Added a read-only full radio admission analyzer over all 95 inventoried MP3 files. It binds inventory, curation, source hashes before and after, FFmpeg versions, complete decode, duration, channel and sample-rate bounds, EBU R 128 loudness and true peak, predicted normalization gain, and leading, trailing, and internal silence. The first run decoded every file without error or source modification, but admitted zero under the provisional policy: 94 miss the loudness band, 91 exceed the true-peak ceiling, 64 have excessive trailing silence, and 3 have excessive leading silence. Analysis cannot approve content or change export eligibility.

- Added a non-destructive station review-copy handoff. It requires the exact full-library analysis, trims only measured edge silence, performs trim-aware two-pass FFmpeg loudness normalization, writes metadata-free lossless FLAC copies outside source, fully decodes and remeasures them, rehashes all 95 sources, and fixes listening, replacement, release, and export approval false. The first 12-track `the_bureau` set passes every technical threshold, leaves every source hash unchanged, and reproduces all twelve output hashes on a second complete campaign. Human listening and exact source-replacement approval remain required.

- Reduced the transient `player-build` Actions handoff retention from 14 days to 1 day because `player-latest` retains the same published bytes. Repository metadata now points package users to the intentionally closed support policy instead of disabled Issues.

- Pointed the Godot watch smoke at the packaged Agent Host. `test_native.ps1` now assembles the current-RID self-contained host, then `Godot_watch_screen_receives_packaged_host_frame_when_qualified` launches the real headless watch route against that executable's viewer pipe. The in-process Godot watch smoke remains. A supported Release export still compiles the watch route out. Host and plugin remain `0.17.0`. Signing, installers, update, rollback, and supported publication remain later AA-10 work. Native contracts are now **1,227**.

- Qualified the unsigned Agent Host package as a live viewer. Native rules CI launches the current-RID self-contained executable, opens a watch-enabled match, and requires a `vibesnake-agent-viewer-frame-v9` stream without writing preview data into the package. `VIBESNAKE_AGENT_USER_DATA_ROOT` isolates that data to an existing fully qualified directory and refuses the package folder. Host and plugin remain `0.17.0`. Native contracts are now **1,226**.

- Retried Windows Coverlet hit-stream truncation after a green native test run. All 1,223 tests can pass while Coverlet still fails with "Unable to read beyond the end of the stream"; that is now treated as a coverage-report failure and gets the existing one rebuild retry instead of being mistaken for a test failure.

- Bound the unsigned Agent Host package to its source revision. `package_agent_host.ps1` now writes `host-inventory.json` from the host lock closure and `host-provenance.json` that hashes the executable, manifest, and inventory. `publication_eligible` stays false. The inventory is NuGet-only, generated from locks, and includes the published RID graph. This is lock-derived SBOM evidence, not a platform signature. Host and plugin remain `0.17.0`.

- Started AA-10 with a current-RID self-contained Agent Host package. `./scripts/package_agent_host.ps1` publishes an unsigned `vibesnake-agent-host-package-v1` that contains the host executable, license files, `INSTALL.txt`, a closed manifest, and complete `SHA256SUMS`. `publication_eligible` stays false. The package smoke-starts over stdio and must not write preview data beside the executable. CI builds and validates it on Windows, macOS, and Linux. The framework-dependent Agent Plugin package is unchanged. Host and plugin remain `0.17.0`.

- Closed AA-07's remaining person-facing surface. A preview-only `--agent-watch-passports` launch lists the local public-identity store in the store's own order. Each row names the agent ID, exhibition count, and best score the receipts earned, and never a display name, because a name is a claim. Ahead, level, and behind stay equal-seed rivalry counts, not a standing. Confirm opens that agent's latest receipt in exhibitions. The screen renders from `vibesnake-agent-passport-browse-report-v1` and never writes. Host and plugin remain `0.17.0`. The copy catalog is 732 entries and 114 parameterized templates.

- Closed AA-08's ranking screen over the existing qualification report. A preview-only `--agent-watch-qualification` launch opens the eight published divisions as pages so standings never look mixed, ranks qualifying results by score inside one division and one policy version, and says in so many words that completed practice and voluntary `finish_match` stay off the list. Rival Breaker copy names published characteristic terms rather than a grade. Confirm opens that standing's best receipt in exhibitions. The screen renders from `vibesnake-agent-qualification-browse-report-v1` and makes no decision the report has not already made. Host and plugin remain `0.17.0`; gameplay, observation, action, result, lesson, style, receipt, viewer frame, replay, passport, and story schemas remain unchanged. The copy catalog is 723 entries and 111 parameterized templates. This is not a claim of fairness comprehension.

- Advanced the Agent Host and Agent Plugin contracts to `0.17.0` with rules resource v18, giving AA-08 a host read path. The new seventeenth tool `get_qualification_report` classifies archived receipts against an immutable eight-division manifest and public practice and qualification-time decks. Voluntary `finish_match` of a running exhibition is never a qualifying result. A completed Signal School practice is practice evidence, not a standing. Standings never mix modes, seed visibilities, observation profiles, action profiles, or policy versions. Rival Breaker beats a named verified rival on published characteristic terms. The eighth resource `vibesnake://agent/qualification` publishes the catalogs without rankings. Building the report never writes. Gameplay, observation, action, result, lesson, style, receipt, viewer frame, replay, passport, and story schemas remain unchanged.

- Closed the remaining AA-09b machine polish. `StyleThresholdFirst` and `LessonRequirementFirst` are now reconstructed from the named agent tape with the same evaluators as the terminal outcomes; `Detail` is the zero-based criterion or requirement index. Attempt-witness lesson requirements have no tape tick and are not first-crossing beats. Confirm on `--agent-watch-exhibitions` still plays the montage, but a lane change is a cut rather than an interpolation from one snake to the other, rival-lane events come from the rival tape, and clean capture hides the story overlay and control panel the same way it hides ordinary replay chrome. Host and plugin remain `0.16.0`; gameplay, observation, action, result, lesson, style, receipt, viewer frame, replay, and passport schemas remain unchanged. This is not a claim of spectator appeal.

- Advanced the Agent Host and Agent Plugin contracts to `0.16.0` with rules resource v17, giving AA-09b a host read path. The new sixteenth tool `get_exhibition_story` builds `vibesnake-agent-exhibition-story-report-v1` from one archived receipt hash and the named lane tapes. Display time is ignored. A missing or disagreeing tape is refused. Building a story never writes the archive, the passport store, or human player data. Gameplay, observation, action, result, lesson, style, receipt, viewer frame, replay, and passport schemas remain unchanged.

- Closed the playtester finding that `--agent-watch-exhibitions` stayed on the main menu. The flag was recognized and then refused: `Menu` to `AgentExhibitions` was not a legal shell transition, so Godot logged `Illegal shell transition from Menu to AgentExhibitions` and left the title screen in front of a real archive. That launch edge is now legal, and so are back to the menu and the same-seed challenge into a run. The smoke graph names those edges so the advertised door cannot close again without a test noticing.

- Advanced AA-09b from a story contract to a watchable recorded-first playback. `vibesnake-agent-exhibition-story-v1` still builds typed highlights, a deterministic turning-point subset, and a montage bound to one verified receipt plus its named lane replays. The montage now covers every tick exactly once: linger holds a turning point, selected plays the beat around it, and skip windows are not played. `vibesnake-agent-exhibition-story-report-v1` loads those named files from the archive, refuses a missing or disagreeing tape before playback starts, and publishes a cursor so a screen never invents a beat, a lane, or a pace. Linger is half the viewer's chosen speed. Confirm on `--agent-watch-exhibitions` now plays that story instead of dumping the raw tape: skip jumps to the next beat, the overlay names the current highlight, lane, pace, lead, and turning point, ordinary replay controls remain, and west/north seek previous and next turning points. Display time is ignored. This is not a claim of spectator appeal. The copy catalog is 699 entries and 107 parameterized templates.

- Advanced the Agent Host and Agent Plugin contracts to `0.15.0` with rules resource v16, closing AA-07's persistent public identity. A caller-declared Passport v4 is still a claim. `record_passport` now folds one verified exhibition into a local `vibesnake-agent-passport-document-v1` store under `agent_arena/agent_passports.json`, assembled only from receipts that recompute their own canonical hashes. Each record keeps policy versions, divisions, exhibition counts, style and lesson tallies, rival ahead/level/behind facts, and four closed milestones that point back at the exhibition that earned them. The store never writes a display name, prompt, credential, or human profile. It is bounded to 16 agents, 32 receipts per agent, and 1,048,576 bytes, writes atomically, quarantines corruption rather than repairing it, refuses a seventeenth agent rather than dropping an existing record, names every forgotten agent, and rebuilds its receipt-hash ledger from the records that remain so forgetting one agent cannot block another agent's later recording. `list_passports` reads without writing and publishes `bytes_used`, `bytes_projected`, both remaining bounds, and each record's position. `forget_passport` removes one record or clears the store. Recording does not require saved replay files and never touches the exhibition archive. Ahead, level, and behind are not standings; AA-08 still owns qualification. Gameplay, observation, action, result, lesson, style, receipt, viewer frame, and replay schemas remain unchanged.

- Completed AA-06 with the exhibition browser screen. A preview-only `--agent-watch-exhibitions` launch opens a list of everything the local archive kept, in the archive's own order so the exhibition eviction will take next is visible rather than buried. Each row names the mode, seed, score, ending, and step count, says whether it was a practice, a style attempt, or a rivalry, and states what can be done with it now: watch, or take the exact same seed as a challenge. A removed replay file is refused before playback starts rather than discovered halfway through, and says which lane went missing. Confirm watches through ordinary verified replay playback; the replay action starts the same-seed challenge in the seeded-challenge score category, which is neither an ordinary human category nor an agent one, so an agent's line can never land in a human leaderboard. The screen renders from `vibesnake-agent-exhibition-browse-report-v1` and makes no decision the report has not already made and tests have not already proven. It is compiled out of Release with the rest of the preview, and its flag is covered by the existing `--agent-watch-` artifact exclusion marker, now asserted directly. The copy catalog is 663 entries and 105 parameterized templates; the maximum-text-scale gate rejected the first draft of the empty-state line at 1,240 pixels against its 1,180-pixel budget, which is the gate doing its job before a playtester had to.

- Added the machine half of AA-06's exhibition browser as `vibesnake-agent-exhibition-browse-report-v1`. It turns the archive into what a person can act on: rows in the archive's own order, which is the order eviction removes them in, so a browser cannot hide which exhibition is about to be lost. Each row says whether it can be watched now and, when it cannot, which lane's replay file went missing; a rivalry needs both lanes. A missing recording never blocks a rematch, because a rematch replays the line rather than the recording and needs only the mode and seed the receipt already published. Selection clamps at both ends instead of wrapping. `vibesnake-agent-exhibition-challenge-v1` is the exact same-seed handoff, and it carries the line and nothing else: no passport, no progression reference, and the agent's score as visible context rather than as a rule. Every challenge runs in the seeded-challenge score category, so a person playing an agent's line produces neither an ordinary fresh-seed score nor an agent one, and the isolation is proven in tests without a running game. A row publishes no display time, because exhibition identity excludes it and a browser sorted by it would present one exhibition differently on every visit. The Godot screen over this report followed in the next unreleased slice; no host contract, stored schema, or gameplay contract changed in this machine-half step.

- Advanced the Agent Host and Agent Plugin contracts to `0.14.0` with rules resource v15, closing a byte-accounting defect a playtester found and answering the two things their round-7 answers still wanted. After a legacy archive was migrated on read, `bytes_used` reported the migrated document while the file on disk was still the old schema, so the two disagreed until the next write. Reading never writes, and it should not start, so the index now publishes both: `bytes_used` is what the file holds and is verifiable against it, and `bytes_projected` is what the next write would produce and is the size the byte ceiling binds, which is why `remaining_bytes` follows it. The same split applies to schema: `schema_version` is the document in memory and `stored_schema_version` is the one on disk, so a pending migration reads as one to two instead of leaving a bare boolean to imply it. Every listed entry now carries its `position` in the whole store and the run's `final_tick`, so eviction order stays visible through a filtered listing and a browser can order and label without opening a receipt. Position and presence are computed at read time; no stored field changed, so archive schema 2 and its migration from schema 1 are untouched. Gameplay, observation, action, result, lesson, style, receipt, viewer frame, and replay schemas remain unchanged.

- Advanced the Agent Host and Agent Plugin contracts to `0.13.0` with rules resource v14, and reshaped the exhibition archive around the round-5 playtest answers. The index was only enough to tell two exhibitions apart when their seeds differed, so an entry now promotes the mode, the end reason, the run status, and the lesson and style identities, and a listing reports whether each named lane replay file is still on disk. Capacity could not be judged from an entry count while a byte ceiling could bind first, so every archive response now publishes `bytes_used`, `maximum_bytes`, `remaining_entries`, and `remaining_bytes`. Eviction reported a count without saying what was lost, so it now names every dropped exhibition by receipt and route hash. Two tools join the archive: `list_exhibitions` reads it without writing and optionally narrows to one `route_identity_hash`, which previously required archiving something to see the index at all, and `forget_exhibition` removes one exhibition or clears the store, which nothing but eviction could do before. `archive_exhibition` now publishes its read-only hint explicitly. The stored schema advances to 2, and a schema-1 archive written by an earlier host is migrated forward on read rather than quarantined: migration is lossless by construction because every promoted field is derived from the receipt schema 1 already stored verbatim, and every rebuilt entry must verify against that receipt exactly as a freshly archived one would. Gameplay, observation, action, result, lesson, style, receipt, viewer frame, and replay schemas remain unchanged.
- Corrected the archive byte ceiling in the `0.12.0` note below, which said one megabyte after the ceiling was raised to four. A playtester found the published changelog disagreeing with rules resource `maximum_bytes`; the resource was right.

- Advanced the Agent Host and Agent Plugin contracts to `0.12.0` with rules resource v13, and closed AA-06's optional two-lane exhibition archive. Until now an exhibition ended when the host process did: a verified receipt existed only while its match handle did, so nothing survived to browse. The tenth tool `archive_exhibition` keeps one verified exhibition in a bounded local store, holding the canonical receipt verbatim beside the saved replay file name of every lane the receipt contains. Archiving is explicit and ordered on purpose: a match must be finalized and verified, and `save_verified_replay` must already have written every lane, because an archived exhibition names files rather than hopes. A rivalry archives both lanes or neither. The store holds at most 32 exhibitions and four megabytes, evicts the oldest first at capacity and reports the exact count, writes atomically so an interrupted write leaves the previous archive intact, and refuses to write a different exhibition under an existing receipt hash. Repeating the call writes nothing and reports `already_archived`. Every stored entry must recompute both of its canonical receipt hashes and agree with each promoted field copied from them; a document that fails is quarantined beside the archive rather than repaired, with the unreadable bytes preserved. Presentation display time is stripped before storage, because display time was never part of exhibition identity. The archive lives outside the supported player Persistence assembly and holds no human score, progression, achievement, cosmetic, or profile data. Gameplay, observation, action, result, lesson, style, receipt, viewer frame, and replay schemas remain unchanged.
- Closed the second half of the run HUD clipping defect. A playtester found that at 150 percent text a high-combo Vibe board stacked `OVERDRIVE` on top of `HUNGER READY`. This was the same defect as the mode-title clip, one cell to the left: every cell of the top HUD strip drew unbounded from its own fixed left edge, which is only safe while the text happens to be short. The row is now a declared table of six cells, each owning where it starts, how much room it has before its neighbour begins, and the smallest type it may use to keep every character; the draw path and the gate read the same table. The readable gate composes the real worst-case English for every cell of both mode presentations from the live Vibe Level catalog, the multiplier ceiling, and every reachable hunger tick of every shipped mode, then requires no elision, a real gutter at every seam, and clearance for the scaling critical-hunger marker. At 150 percent text no cell falls below 17-point type. Both failure modes were reproduced against the new gate before the fix was accepted.
- Advanced the Agent Host and Agent Plugin contracts to `0.11.0` with `vibesnake-agent-viewer-frame-v9` and rules resource v12, closing AA-03's observed exit, risk, and recovery-resource lane. A viewer frame now carries `vibesnake-agent-survival-state-v1`, and the watch overlay gained a survival row that names the structural open exits out of three non-reversal candidates, the closed pressure tier that count crosses, and the four held recovery resources in a fixed order. The block is spectator legibility, not route advice: it says what is true now and never names a direction to take. The agent observation is deliberately unchanged, because an agent can already derive every one of these facts from the board it receives. The host derives its copy from the authoritative run snapshot while the viewer independently recomputes all of it from the public observation, using the same structural-exit definition the Stillwater style criterion measures, and rejects a frame that disagrees with its own board. Gameplay, observation, result, lesson, style, receipt, and replay schemas remain unchanged.
- Closed the run HUD title clip a playtester found at 150 percent text. The top-right mode title drew unbounded from x=980 on a 1280-wide canvas, so `CLASSIC AGENT COMPLETE` lost its final letter and read `CLASSIC AGENT COMPLET`. The same slot also clipped the ordinary human `PAUSED: FOCUS LOST` status, which no playtest had reached. The title now declares an explicit width budget and shrinks its own type before it would drop a character, and a readable run-HUD gate composes every mode and status pairing at maximum text scale and fails on any elision. The run statuses `RUNNING`, `DEAD`, `WON`, `PAUSED`, and `PAUSED: FOCUS LOST` moved from raw literals into the copy catalog, which is what lets the gate compose real English; which took the catalog to 639 entries.
- Closed the last watch-overlay elision. At 150 percent text the finish row still collapsed `OUTCOME` to `..UTCOME`, because the long viewer status sentence shared that row. The composed overlay now uses short feed labels while the full status sentences stay on the standalone waiting screen, where the disconnect promise about host match control still reads in full.
- Added a readable-overlay gate that fails when realistic English content elides. The existing geometry gate proved a row fits after grapheme-safe middle elision, which is not the same as a spectator being able to read it, so two rounds of `A..TEP` and `..UTCOME` passed CI. The new gate composes the actual worst-case English for the verification and outcome rows using the real step cap and mutation ledger bounds, and asserts no elision at maximum text scale. It reproduced the reported defect before the fix.
- A failed-closed match now shows `REPLAY UNAVAILABLE` instead of `REPLAY PENDING`. That match will never receive a verified replay, so pending was a false promise.
- Gave the Agent Watch verification facts their own overlay row. A playtester found that at default text scale the finish row already middle-elided its outcome phrase to `A..TEP` while sharing space with the seed, state hash, and replay hash, and 150 percent text has less room rather than more. The evidence panel grew by one row, the seed and both hash prefixes moved to a dedicated line above the identity row, and the composed pseudo-localized geometry gate covers the new layout at 150 percent text.
- Added the preview-only `--agent-watch-accessibility` flag so overlay legibility can be observed at maximum text scale and high contrast without keyboard injection. The published route is the F6 and F9 hotkeys, which an automated spectator cannot press, so this presentation profile was previously unobservable from outside. It requires the viewer capability, is excluded from supported Release artifacts by the existing `--agent-watch-` marker policy, and is compiled out of `ExportRelease`.
- Documented that a head-to-head comparison requires equal `division_id` and equal `gameplay_seed`. A division identity names the ruleset division, not the board, so equal `division_id` alone would score two different boards against each other.
- Fixed the documented Linux first launch, which never opened a window. The published source archive carried `play.sh` without an execute bit, so `./play.sh` failed with exit 126 before reaching any launcher logic. The shell entry points are now tracked executable and a repository test asserts every shebang script keeps mode `100755`.
- Fixed `play.ps1`, which could not be invoked on any platform. It was an advanced script, so under `Set-StrictMode` a no-argument run failed with "The variable '$args' cannot be retrieved because it has not been set", and the documented watch handoff `./play.ps1 --agent-watch-pipe=... --agent-watch-token=...` failed with "A parameter cannot be found". It is now a simple script that captures forwarded arguments before any other work, accepts an optional `--` separator, and forwards them to Godot. A PowerShell gate executes the real launcher in all three argument shapes and fails when argument binding breaks.
- Advanced the Agent Host and Agent Plugin contracts to `0.10.0` with `vibesnake-agent-exhibition-receipt-v2` and rules resource v11. A receipt now publishes two identities: `receipt_hash` names this exhibition instance and binds the match handle, while the new `route_identity_hash` names the walked line and omits the match handle, the caller-declared passport, presentation events, and any attempt evidence derived from idempotency keys. The same division, seed, and verified replays therefore reproduce `route_identity_hash` across separate matches and separate host processes, including `first-turn`, so a same-seed rematch is recognisable. Gameplay, observation, lesson, style, replay, and viewer schemas remain unchanged.
- Fixed a first-launch defect on the documented player path. A clean clone and the published source archive both ship the committed `*.import` descriptors without the generated `.godot/imported/` payloads they name, and Godot cannot write that cache while running the game, so `./play.ps1` died loading `res://assets/branding/vibe-snake.png`. The new `scripts/assert_godot_import.ps1` verifies every declared import destination, performs one bounded headless import when any is missing, re-verifies, and fails closed; `play.ps1` runs it before launching, and a PowerShell gate executes it against the real project.
- Advanced the Agent Host and Agent Plugin contracts to `0.9.0` and began AA-06. The new ninth tool `get_exhibition_receipt` returns `vibesnake-agent-exhibition-receipt-v1`: a transport-neutral canonical receipt whose `receipt_hash` binds the match identity, a `vibesnake-agent-division-identity-v1` block, the public passport, lifecycle and end reason, the revealed seed, the terminal facts, both verified lane replay payload hashes, the replay-derived style and lesson evidence hashes, and the ordered accepted presentation events. `display_time_utc` rides beside that hash and is deliberately excluded from it, so one exhibition keeps one identity whenever it is shown. A receipt exists only for a successfully finalized, verified match, and a rivalry is receipted only when both lanes verified independently. Rules resource v10 publishes the `receipt` block.
- Closed the remaining request from the `0.8.2` playtest. Viewer frame v8 publishes `verified_replay_payload_hash` alongside a verified result, and the Agent Watch overlay prints `REPLAY` with its first eight characters or `REPLAY PENDING` while a match is live. The viewer client rejects a replay identity that is absent from a verified frame, present without one, not lowercase hex, disagreeing with a bound style or lesson outcome, or changed on a later frame. Gameplay, observation, lesson, style, and replay schemas remain unchanged.
- Advanced the Agent Host and Agent Plugin patch contracts to `0.8.2` after the `0.8.1` regression playtest confirmed the move-tool bind fix and reported the remaining protocol-clarity friction. The call-tool request filter now covers all eight tools and validates JSON value types, so an integer `gameplaySeed` is named as a wrong-typed field with a quoting hint instead of collapsing into a generic SDK bind error; the official stdio transcript proves the rejection and the recovery with `"42"`. Rules resource v9 publishes `lifecycle_semantics` and `argument_binding`, stating that `lifecycle` describes the agent session while `run_status` describes the snake, that a completed lesson on a living snake is correctly `completed` with `run_status: running`, that `is_action_awaited` stays true because satisfying requirements never ends a match, and that a requirement's `satisfied` flag is evidence rather than a grade. The Agent Watch overlay now prints the match seed, or `SEED BLIND` in the blind division, and the first eight characters of the current step's state hash, both read from the then-current viewer frame payload. Gameplay, observation, lesson, style, replay, and viewer schemas remain unchanged.
- Advanced the Agent Host and Agent Plugin patch contracts to `0.8.1` after the `0.8.0` regression playtest verified every prior lifecycle, retry, curriculum, style, and death-route fix. The pinned MCP SDK's call-tool request filter now validates raw `play_move` and `play_burst` argument names before method binding, identifies missing and unexpected fields, and confirms that no match state changed. The official stdio transcript proves both reported wrong-field cases preserve tick and state hash and recover on the same handle with the discovered camelCase field names. Gameplay, observation, lesson, style, replay, and viewer schemas remain unchanged.
- Closed the remaining actionable findings from the follow-up AgentHost playtest and advanced the Agent Host and Agent Plugin contracts to `0.8.0`. Signal School progress and outcomes now use v3: a completed live lesson recommends `finish_match`, a successful `target_reached` outcome omits retry guidance, and only incomplete or failed-closed practice offers a fresh-session retry. Style progress and outcomes now use v3 threshold terminology so raw booleans cannot be mistaken for grades. The `death-read` instruction names its deterministic self-collision route and states that starvation exceeds the practice cap. Rules, Signal School, and style catalog resources advanced to v8, v4, and v3. Exact argument names and the SDK pre-invocation binding boundary remain explicit for both move tools.
- Closed three findings from an agentic AgentHost playtest and advanced the Agent Host and Agent Plugin patch contracts to `0.7.1`. `finish_match` now reports `completed` when a Signal School lesson has satisfied all requirements while retaining `aborted` for partial lessons and ordinary nonterminal early exits. The viewer enforces that exact lifecycle and lesson-evidence relationship. Public tool, resource, skill, and engineering guidance now names the required `play_burst` camelCase arguments and describes Style Contract criteria as factual measurements against optional targets, not pass/fail match grades. The host cannot rewrite SDK parameter-binding errors that occur before tool code runs, so recovery guidance directs callers back to the discovered schema. Locked interaction-byte measurements, generated knowledge, interoperability digests, focused regression tests, and full qualification evidence were refreshed without treating agentic observations as human learnability or fun evidence.
- Replaced Signal School's six scalar-target practices with eight ordered two-requirement lessons for the post-1.0 Agent Arena preview. First-turn binds one valid zero-step reversal rejection to later verified direction-change evidence through a separate maximum-32 attempt-witness sequence; wrap, hunger, exit, power, recovery, combo, and death lessons use independently reconstructed replay facts. Exact retries never duplicate evidence, successful outcomes bind replay and attempt hashes, incomplete and failed-closed reviews provide only a fresh same-lesson retry, and 16 canonical lesson/profile records publish exact action-call, MCP arguments JSON, and structured-response UTF-8 measurements. Observation-derived bounded burst routes never use more action calls than paired step routes and reduce calls for at least six of eight lessons. Advanced the preview to Passport v4 with `symbolic-step-v4`, observation and result v5, viewer frame v7, Signal School resource v3, rules resource v7, identity resource v3, and Agent Host and Agent Plugin `0.7.0`. Replay schema 1, `vibesnake-core@4`, action profiles, style contracts, human data, and the application version remain unchanged. These public practice fixtures do not prove mastery, generalization, or human spectator appeal.
- Hardened the Agent Arena truth boundary so the viewer rejects missing, duplicate, unknown, or noncanonical JSON fields; impossible episode and mode facts; catalog-drifting lesson, identity, or rival facts; contradictory style arithmetic; and mismatched terminal style or lesson outcomes before presentation. The watch overlay distinguishes live lesson progress, verified combined evidence, failed-closed evidence unavailability, replay-verified style truth, and post-terminal zero-step rejections without erasing the completed outcome. Host input parsing applies the same required-member, duplicate-member, closed-enum, and unknown-member discipline. The authoritative native qualification passes 1,091 tests with 94.57 percent aggregate line coverage and 87.42 percent aggregate branch coverage; every measured module remains above the 90 percent line and 85 percent branch floors.
- Added AA-03b replay-derived composite Style Contracts for the post-1.0 Agent Arena preview. Every style now exposes exactly two ordered factual criteria with closed units and comparisons: Stillwater combines survival with structural-open-exit rate; Crownchaser combines peak combo with uninterrupted pre-peak continuity; Edge Prophet combines rewarded body-proximity near misses with a same-step wrap fact under the pinned `vibesnake-core@4` evaluator; Mutagenist combines distinct activated power kinds with concurrent active kinds; and Redline combines food count with safe progress toward the exact pre-step visible food. Live values remain observed rules-advanced-step evidence and may rise or fall. Only successful finalization returns a style outcome independently reconstructed from, and hash-bound to, the verified replay. Advanced the public preview to Passport v3 with `symbolic-step-v3`, observation and result v4, viewer frame v6, style catalog v2, rules resource v6, identity resource v2, and Agent Host and Agent Plugin `0.6.0`. Replay schema 1, `vibesnake-core@4`, action profiles, human data, and the application version remain unchanged. The criteria do not prove intent, planning, mastery, personality, or spectator appeal.
- Added the AA-03a catalog-bound identity foundation for the post-1.0 Agent Arena preview. `vibesnake-agent-passport-v2` keeps caller-declared agent, policy, and display identity while restricting avatar, accent, and station presentation to closed public catalogs. Observation v3 and viewer frame v5 reject legacy, mixed, unknown-catalog, and identity-drifting payloads. The Godot watch route resolves an agent-owned avatar and accent independently of human cosmetic progression, while station identity remains presentation metadata rather than content approval. Advanced the Agent Host and Agent Plugin preview contracts to `0.5.0`; replay schema 1, `vibesnake-core@4`, action profiles, human saves, and the application version remain unchanged. Composite replay-derived Style Contract evaluation remains the next AA-03 tranche.
- Completed the AA-09a automated live-truth layer. `vibesnake-agent-viewer-frame-v4` identifies initial, step, burst, and finish operations; binds exact advancement to the pre-mutation tick and state hash; publishes burst stop reason and event, terminal truth, and monotonic source sequence; and labels every preflight burst rejection as a zero-step burst. The latest-frame client reports every sequence gap as coalesced earlier updates, rejects identity drift and contradictory advancement, stop, event, action, lifecycle, and outcome facts, and clears pending content after malformed or oversized wire input. The exact packaged `mcp.json` transcript now opens the viewer and completes a terminal burst on every hosted OS, while a server-retention test proves newest-unsent delivery. The overlay says when agent deliberation has paused rules, reduced-motion mode demonstrably snaps rather than interpolating a multi-step jump, and oversized rows use grapheme-safe middle elision so composed pseudo-localized geometry remains inside the logical canvas at 150-percent text. A real Godot pipe smoke requires the terminal three-step burst under muted, high-contrast, reduced-motion, and 150-percent-text settings without claiming that human spectator appeal is proven. Advanced Agent Host, rules resource, Agent Plugin, and skill preview contracts to `0.4.0`.
- Added the supported-player Agent Arena exclusion gate. Godot `ExportRelease` now omits AgentPlay and AgentViewer project references and compiles out the watch route; artifact inspection rejects AgentPlay, AgentViewer, AgentHost, plugin, knowledge, skill, MCP, and interoperability-baseline payload paths plus compiled command-line markers. Artifact manifests advance to strict schema 3 with a Release-only `agentArenaPreviewExcluded` assertion. Exact Release run 32421705560 retains passing schema-3 manifests with `agentArenaPreviewExcluded: true` on Windows x64, macOS Universal, and Linux x64.
- Added the dedicated `start_lesson` MCP tool and six canonical Signal School practices with lesson instructions, live progress, exact mutation deltas, first-target burst stops, replay-derived hash-bound outcomes, and one locked successful replay plus initial-shortfall fixture per published route. Advanced the Agent Host and Agent Plugin preview contracts to `0.3.0`, the lesson-aware match schemas to v2, and the viewer frame to v3. The viewer now distinguishes target hit, verified, and failed-closed unverified states with stable localized metric labels. This primary-target practice slice does not claim all eight observable behaviors or a future qualification-time deck.
- Hardened the assembled Agent Plugin boundary: packaged validation now requires every fixed component, exact `dotnet` host declaration, one-token command, contained working directory, and complete `SHA256SUMS`. Windows, macOS, and Linux CI parse the generated `mcp.json`, expand its package-root placeholders without a shell, and run the official C# SDK transcript through that exact declared launch.
- Before the 0.3.0 lesson contract, completed the Agent Arena viewer's closed rejection copy for wrong-profile and exhausted-mutation-budget responses, corrected disconnect status so it never implies that an unfinished or failed-closed match already has a verified replay, and advanced the Agent Plugin guidance-only contract to `0.2.1`.
- Audited the canonical documentation against current source, workflows, release contents, and live repository policy. Corrected Agent Arena watch and recovery guidance, Signal School and style claims, quality commands, localization counts, security intake status, and the dependency order for burst-specific spectator polish.
- Added the AA-04 efficient symbolic-control preview. `four-direction-burst-v1` applies one initial action, continues for at most 16 clock-free steps, and stops on the fixed `decision-event-stop-v1` public-event catalog, rules terminal, match cap, replay failure, or requested bound. It returns actual advancement and final-step truth while remaining a separate control division from exact one-step play.
- Unified step and burst mutation idempotency so one key can never advance both operations, proved concurrent exact retries and equivalent final state, replay, metrics, and equal-seed rival state, and kept burst viewer output to one bounded final frame without changing canonical replay history. Each session retains at most 4,096 unique mutation records; known retries remain authoritative while every unseen key fails closed after exhaustion.
- Added capacity-only abandoned-session recovery: finalized sessions are evicted first, while an all-live eight-session host may reclaim only a handle with no valid handle-bearing host operation for 30 minutes. Reclamation produces no score, result, ranking, or replay, and viewer activity never refreshes or controls the lease. A replacement is fully validated and constructed before any existing session is evicted.
- Made terminal replay finalization transactional with step and burst idempotency. Agent or rival recording and verification failures now return one cached typed `replay_failure` response with truthful advancement and publish the failed-closed viewer frame instead of escaping before the mutation is remembered.
- Earlier in this development cycle, bumped the Agent Host and Agent Plugin preview contracts to 0.2.0 for the seventh MCP tool and action-profile change. Corrected MCP 2026-07-28 documentation to its stateless, no-initialize model and added a legacy-client rejection transcript. One machine-readable interoperability baseline now owns reviewed versions, schema digests, and review dates; ordinary CI validates it offline and a read-only weekly workflow verifies pinned Agent Plugins specification and schema bytes.
- Corrected OKF 0.2 `stale_after` to the required absolute `YYYY-MM-DD` form and added an executable as-of freshness gate that fails on the declared date until source review metadata advances.
- Added a post-1.0 Agent Arena developer preview without adding it to the supported 1.0 release contract. The preview includes a Rules-only transport-neutral match service, closed public logical-state observations, exact one-step idempotent actions, open and blind seed divisions, verified replays, five Style Contracts, six Signal School lesson definitions with deterministic metric evaluators, bounded public Agent Passports, five closed presentation-only public intents, and optional equal-seed built-in rivals.
- Added a local stdio MCP 2026-07-28 host with six original schema-bound tools and six resources, bounded opaque sessions, explicit path-free replay save, protocol transcript coverage, and no network listener. Later preview slices added `play_burst` and `start_lesson`, bringing the current host to eight tools while the same-user one-time named-pipe capability continues to feed a read-only Godot watch screen without giving the viewer match control.
- Established the earlier `vibesnake-agent-viewer-frame-v2` contract with explicit action acceptance, rejection reason, exact end reason, failed-closed state, and verified-result availability. The Godot watch screen added passport color, shed, and station, while viewer disconnects no longer implied that a replay already existed. The 0.3.0 work superseded this wire identity with v3, and the current 0.4.0 work supersedes v3 with operation and delivery truth in v4.
- Added a minimal non-experimental Agent Skill and Agent Plugins 1.0.0 preview package with deterministic producer-profile validation and checksums, plus a generated Open Knowledge Format 0.2 bundle derived from canonical rules and protocol sources. OKF source provenance no longer mislabels CI as the source author, and deterministic content-change, verification, and quarterly staleness dates make review obligations explicit. The formats remain optional interoperability surfaces rather than gameplay authorities.
- Reordered Agent Arena delivery so bounded symbolic burst precedes long curricula, a hash-linked exhibition receipt precedes history and handoff, and learning, identity, qualification, spectator polish, packaging, visual control, and optional ecosystem surfaces follow their actual dependencies. Human review remains a parallel evidence lane while reversible engineering and polish continue through one qualified `main` revision and refreshed `player-latest` release at a time.
- Tightened the earlier Agent Arena preview claims: passport shed and station values were labels pending closed-catalog avatar rendering; terminal output lists the actual ten-metric vector; rejection responses clear prior events; ordinary replay hashes verify each lane but do not constitute the planned exhibition receipt; and human review remains required before legibility, expression, or drama is claimed as achieved. The later AA-03a entry supersedes that placeholder passport presentation.
- Hardened the isolated `player-latest` publisher against partial GitHub API failures. It now reconciles and verifies the floating tag independently, removes only the old release object, retries bounded publication attempts, and refuses success unless the tag resolves to the exact CI-qualified revision.
- Removed the floating source publisher's manual-dispatch checkout path after CodeQL identified it as a potential cache-poisoning boundary in a workflow that later receives a write token. Only a successful same-repository `main` push can now supply source artifacts to publication.
- Enabled three-language GitHub CodeQL default setup, restricted repository Actions to GitHub-owned commit-SHA-pinned actions, and added one aggregate `CI complete` branch check behind an active deletion and non-fast-forward ruleset with an explicit repository-admin bypass policy.
- Stabilized replay-store concurrency qualification under instrumented Windows runners by giving the idempotency and capacity race tests an explicit 30-second acquisition window. The player-facing two-second busy contract and its dedicated contention test remain unchanged.
- Synchronized README, testing, architecture, quality, packaging, and release-status documentation with the then-current 615-test Python baseline, 1,002-test native coverage report, MCP 2026-07-28 and official C# SDK 2.2.0 pin, the Agent Plugins 1.0.0 status discrepancy between its Published normative repository and Working Draft website, Open Knowledge Format 0.2 boundary, and the exact source-versus-assembled contents of `player-latest`.
- Enabled GitHub vulnerability alerts and unpaused Dependabot security updates, while retaining release-blocking locked Python and NuGet audits and isolated review of every proposed dependency change.
- Corrected Agent Arena truth surfaces to distinguish the agent overlay from local rendering, near-miss event counts from intentional risk, verified lane results from future exhibition receipts, bounded display-name validation from broader text safety, and successful finalization from failed-closed replay absence. The roadmap now includes abandoned-session recovery, voluntary-finish eligibility, compact improvement feedback, slow and fast live pacing, and complete recorded-viewer controls.
- Advanced the development identity to canonical SemVer `0.3.0-alpha.1`, mapped deliberately to Python package version `0.3.0a1`, with one root `VERSION` authority and a drift gate across native and Python identities.
- Removed versioned tag publication from the continuous Python source-snapshot workflow. `player-latest` remains the explicit source/reference channel from `main`.
- Added fail-closed unsigned native alpha assembly. It requires one matching three-platform Release matrix, deterministic qualification packages, exact manifests and checksums, detached provenance, canonical alpha tag identity, and a release-ready content inventory before GitHub prerelease publication.
- Kept every qualification and stable output `publicationEligible: false`; alpha downloads are renamed `-unsigned-preview` and carry an aggregate manifest, checksums, provenance, and explicit signing and compatibility limitations.
- Stabilized shared-host native qualification after an Ubuntu runner measured 993.4 rules steps per second against the former 1,000-step floor and then exhausted the job timeout by rerunning all 892 tests. The host-dependent floor is now a conservative 750 steps per second, the job permits normal runner variance, and only a missing or malformed coverage report can trigger the one clean rebuild retry. A real test failure now fails immediately.

### Changed

- Expanded release dependency provenance to all nine committed NuGet locks plus both Python locks, including the Agent Host, Agent Play, Agent Viewer, and creator validator. Inventory and artifact generation now compare the SDK that actually runs with the exact toolchain pin before recording it.
- Installed the existing hash-locked CI tool graph in each native job before cross-platform Agent Plugin validation, so the strict YAML frontmatter parser runs with the same reviewed dependency graph as the Python quality job.
- Bounded Agent Arena viewer pipe names to a portable 24-character contract with 80 bits of random identity, added a ten-second fail-closed connection deadline, and made malformed-frame tests delivery-aware so macOS socket paths and loaded Windows runners remain reliable. Real MCP subprocess and named-pipe integration tests now run in a non-parallel collection with explicit process-startup and discovery budgets so loaded hosted runners cannot turn process starvation into a false protocol failure.
- Hardened native failure handling so failed or canceled replay saves, settings recovery, data reset/restore, progression and cosmetic persistence, diagnostics/backup folder opening, and audio cleanup remain visible and fail closed instead of reporting success or releasing a pending quit. Read-only artifact qualification now accepts only the intended access-denied failure.
- Hardened the optional-pack recovery and alpha assembly boundaries. Quarantine now refuses to exceed its 256-pack recovery limit, restore refuses to exceed the 128-pack installed limit, both operations return typed busy and I/O failures without moving data, and invalid receipts do not create storage. Release evidence JSON is size-bounded, duplicate-free, finite, and type-strict, radio station identity and track counts are cross-checked exactly, and the frozen Python oracle no longer prints every main-menu key press.
- Refreshed the qualified development and CI toolchain against generally available releases on 2026-08-13. Godot remains on 4.7.1 stable; the exact .NET 10 LTS SDK advances to security-servicing release 10.0.303, and `Microsoft.Extensions.Hosting` advances to 10.0.11 with its locked transitive graph. Preview Godot 4.8 and .NET 11 builds remain excluded. GitHub Actions are pinned by commit to their current stable releases, .NET test and coverage packages are current, and both hash-locked Python graphs were regenerated from current stable packages. The latest completed three-platform hosted Coverlet 10 baseline measures Rules at 95.77/88.43 percent and Persistence at 93.53-93.55/87.12 percent line/branch, so the current alpha gate remains 90 percent line and 85 percent branch per measured module; the roadmap's 0.4 gate remains 90 percent branch coverage.
- The root launchers now start the native Godot and C# product from source. The Python implementation remains available only as the frozen behavioral oracle.
- Reworked the README into a concise project front door that routes design, engineering, player, and release detail to canonical documents. Updated those documents to match the native source default, current controls, persistence schema, validation counts, and release posture.
- Replaced the legacy README media with four deterministic 1280x720 native captures covering the main menu, Vibe gameplay, customization, and the compact AI channel. The capture verifier now fingerprints the native presentation sources and rejects stale or incomplete screenshot sets.
- Reduced the active Let's Play overlay from a 106-pixel dashboard to a translucent 44-pixel two-line broadcast ticker. Control prompts appear briefly at start or after pointer, keyboard, or controller interaction, auto-hide after three seconds, and remain while paused or complete. Measured HUD columns keep ordinary radio station and track names visible without collisions.
- Separated shared-host stress qualification from focused bare-loop pacing. The three stress profiles retain a 25 ms sustained-average ceiling and use a 70 ms p95 gross-regression envelope that covers the observed shared macOS timer-batching mode; the later minimum-effects bare loop keeps its stricter 60 ms p95 and 100 ms hard-frame ceilings. Neither CI envelope establishes 60 FPS: the 16.67 ms target remains accepted only on named hardware. Every bare-loop attempt and every stress profile stabilizes for 30 unmeasured process frames before measurement so known exported-player startup, JIT, renderer initialization, profile transitions, and shared-runner scheduling do not contaminate the retained distribution. Exported smoke retains its bare-loop burst after the complete three-profile qualification sequence, so the exercised runtime and rendering paths complete their stabilization sequence before the focused sample begins. The performance evidence publishes and validates its 30-frame stabilization and 40-frame measurement counts. A profile with a tail-only shared-runner spike may be measured once more without discarding already-passing sibling profiles; every profile still must pass its ceiling, and structural, sustained, or repeated failures remain fatal. Percentile interpolation is clamped to its source samples so floating-point rounding cannot report p99 above the observed maximum.
- Unified local and hosted native coverage execution behind one module-validating gate with a single clean rebuild retry for truncated Coverlet hit streams.
- Roadmap, README, release status, technology strategy, and native README now state the product path explicitly: Godot + pure C# ship; Python is a frozen oracle only. The next-work order now starts with one retained three-platform Release build, physical product evidence, export-pack approval, and the first native alpha, with bounded .NET replacement of Python tooling only as parallel work.
- Input, viewport, presentation, audio, content, replay, onboarding, run-end, player-data recovery, balance, development, testing, and release documentation now matches the verified controller-remap, vector-prompt, scaling-matrix, audio-mixing/recovery, strict-pack, signing-readiness, deterministic-package, power-decision, replay-browser, AI-personality, progression, content-curation, creator-validation, offline-comparison, and then-current 1,002-test native baseline.
- Added a pure broadcast policy for eight explicitly unapproved station identities, four safe host boundaries, event-aware ducking, critical-cue priority, captions, fatigue limits, and no-repeat host selection. Radio track choice now uses per-station shuffle bags that exhaust playable tracks and prevent immediate repeats across refills.
- Added stable `classic@1` and `vibe@1` product-mode contracts with separate fair-score categories, exact mode factories, remappable keyboard/controller menu selection, and retained `mode-contract-qualification-v2` evidence. Classic disables starvation, combo/speed/length bonuses, near misses, powers, progression scoring, and adaptation in rules rather than presentation only. Config identity advances to `sha256-canonical-runconfig-v3` so mode, score-model, and DDA policy fields cannot mix categories.
- Added deterministic Vibe policy `vibe-bounded-hunger-v1`. Support drains hunger every other step below combo 3 in the warning band, Pressure adds one hunger tick every fourth step at combo 10 or higher outside that band, and Standard drains normally. The live HUD and run-end score metadata disclose state and policy. Preferences schema 5 adds a keyboard/controller-accessible opt-out and isolates `vibe-standard-v1-dda-on` from `vibe-standard-v1-dda-off`; `adaptive-fairness-qualification-v1` proves bounds, determinism, replay restoration, metadata, preference round-trip, and category isolation. Current native run-local achievements are explicitly Vibe-only pending the V070-08 per-achievement audit.
- Added `balance-laboratory-v1` with nine deterministic routing, stress, personality, and replay policies; ten hostile scenario probes; 324 paired runs; 27 descriptive distributions; 124,242 compared steps; seven verified outlier replays; state hashes; first-divergence evidence; and reviewed fixed, exploratory, and previous-failure corpora.
- Added a hash-locked observed balance baseline over 100 reviewed seeds, three fair-score variants, nine policies, and 2,700 runs. Per-run and distribution evidence records score, survival, length, food rate, death causes, combo peak, power encounters, pickups, activations, and outcomes. Six policies are classified as reference AI, while human target ranges remain explicitly empty.
- Added the pure C# `native-personality-controller-v1` catalog and AI league. All ten built-in personalities run across the same twelve reviewed seeds in 120 simulations and 99,358 paired deterministic steps. `native-ai-league-v1` records the seven roadmap metric families by personality and rules version, performs 60 same-state trait interventions, flags fifteen low-materiality personality/trait pairs for truthfulness work, and proves every AI score identity is noncompetitive without constructing human score persistence.
- Advanced the native AI policy to `native-personality-controller-v2`. Reviewed broadcast names and descriptions now match ten explicit measured behavior claims; risk can actively accept hazards or preserve exits, `greed` changes food routing and power detours, chaos remains bounded, and all 60 trait interventions exceed the 1 percent materiality floor over 98,984 paired steps. The strict shared custom schema rejects unknown, duplicate, oversized, unsafe, reserved, non-finite, out-of-range, and invalid-color content with source-specific reports. Typed spectator snapshots expose target, risk, policy, current reason, five recent decisions, and unambiguous built-in versus `CUSTOM / UNOFFICIAL` status through `ai-personality-qualification-v1`.
- Advanced opted-in local playtest summaries to schema 2. The exact 26-field balance-only record adds nine catalog-ordered per-power aggregate rows for offer, detour, collection, activation, expiry, consumption, save, and death adjacency, while excluding identity, raw input timing, device and system details, paths, and free text. Identity-verified schema-1 records migrate without weakening the newest-200, 512 KiB, newest-20 export, default-off consent, local-only, and separately confirmed permanent deletion boundaries.
- Expanded the hash-locked structured-human-playtest protocol to fifteen scenarios and nineteen observation fields. The six added power cases cover Boost plus Phase Shift, Slow-Mo plus Magnet, Bait plus Boost, Gluttony plus Magnet, Segment Detach plus protection, and Last Stand after a long combo. The automated handoff now requires eleven retained artifacts and deliberately reports zero human sessions, unverified experience, and no human target ranges until real participant evidence is reviewed.
- Added `power-decisions-v1`: all nine product powers are reachable in Vibe under protection, tempo, harvest, and geometry anti-redundancy rules. Pre-collection offers retain family, marker, effect, duration, and held-state presentation beside active powers. `power-decision-qualification-v1` locks deterministic reachability, compatibility, lifecycle aggregates, six seeded synergy scenarios, and config identity. The deterministic two-choice Mutation Fork prototype remains default-off, unwired, and human-unverified.
- Added a strict balance-experiment registry and automated guard. Seven eligible balance families require target ranges before any change, exactly one family per experiment, a declared competence/autonomy/tension/recovery effect, fixed-corpus and human evidence, exact identities, and keep/revert/blocked review. The passing initial state contains zero targets and experiments and cannot authorize tuning from average score alone.
- Added a closed eight-context score-purpose and seed-origin taxonomy. Personal-best schema 2 now persists explicit mode, run kind, seed category, score category, difficulty, DDA, config, and display identity; only normal human and separately categorized seeded challenges are competitive, while schema 1 migrates visibly to noncompetitive `Legacy 0.2`. A hash-locked audit accounts for all 25 reference achievements, retaining 17 explicit Vibe-only native candidates and documenting every Classic or reference-only exclusion.
- Added schema-1 native top-ten history per exact score category and a player-facing Local Scores browser. Keyboard V or Down and controller Down open the same screen; category navigation, confirmation, cancellation, and import work through both input families. A confirmed one-time import reads only `user://imports/high_scores.json`, records its SHA-256, preserves the source, sanitizes bounded local labels, and presents every imported row as noncompetitive `Legacy 0.2`. Local-score recovery owns both `personal_bests.json` and `score_history.json`.
- Native coverage gates now require at least 90 percent line and 85 percent branch coverage for every instrumented module in local and hosted CI runs, and reject reports that omit Rules, Persistence, AgentPlay, AgentViewer, or AgentHost.
- Input binding validation rejects non-finite controller-axis values, and release artifact parsing rejects duplicate or unknown fields at every schema level.

### Added

- Added `candidate-reliability-qualification-v1`. Packaged smoke now mirrors 100,000 balanced-AI steps in each of Classic and Vibe and requires identical decisions, queue outcomes, ordered events, state hashes, and a null first divergence. It also advances 100 fresh spectator sessions, verifies reset state and managed collection, and retains eleven stable Godot node/object/resource/orphan samples. A failure now retains first-divergence seed, run, step, hashes, and recent-command evidence before the candidate fails.
- Added `candidate-fault-campaign-v1` through the real packaged smoke. Seven production-boundary probes cover interrupted writes, corrupt JSON, disk-full errors, read-only player data, missing resources, invalid content packs, and unavailable audio while requiring committed-data preservation, recovery, and unchanged rules. Privacy-safe synthetic crash and deterministic-divergence reports verify the local triage path. Exact Release run 32421705560 passes and retains all 21 fault rows plus crash and divergence triage on Windows, macOS, and Linux.
- Added `performance-qualification-v1` to every release-matrix row. The aggregate requires 360 ordered live-frame samples, exact minimum/default/maximum-safe stress shapes, reviewed shared-host ceilings and presentation budgets, and one rules-state hash across Windows, macOS, and Linux. Named-hardware 60 FPS acceptance, resolution captures, long-session resource review, and minimum/recommended specification publication remain open.
- Added `candidate-accessibility-audit-v1` to packaged smoke and every release-matrix row. The fail-closed aggregate SHA-256-binds seven source records, locks twelve ordered roadmap areas, requires independent keyboard/controller remapping and single-action routes, separated audio and mono output, multimodal alternatives, reduced motion, zero full-screen flashes, and 150 percent text across eight display classes. The matrix requires three platform audits and 24 display rows. The new accessibility feature guide publishes exact native support while physical-device, retained-platform, photosensitivity, and accessibility-user review remain open.
- Added the V090-07 `manual-product-matrix-handoff-v1` contract and validator. Four exact Windows/macOS-Apple-Silicon/macOS-Intel/Linux rows cross 36 required flows into 144 cells, with keyboard, mouse, Xbox-layout, PlayStation-layout, eight settings profiles, candidate/artifact identity, and safe retained evidence requirements. Zero manual sessions are recorded, so execution and release acceptance remain false. The native shell now provides nine scaled mouse menu targets, left-confirm and head-relative steering, middle-pause, right-Back, two-axis wheel navigation, and letterbox rejection with packaged `mouse-input-qualification-v1` evidence.
- Added the V090-08 `external-validation-handoff-v1` contract and validator. Four participant cohorts cross three artifact platforms, four input classes, six fresh-participant comprehension checks, four structured report families, exact clean candidate and artifact identities, retained files, finding closure, and affected-gate reruns. CI records zero external candidates, sessions, findings, and crashes, so execution and release acceptance remain false until the controlled group runs.
- Added the V090-09 `release-materials-handoff-v1` contract and validator plus privacy, recovery, known-issues, credits, and third-party-notice foundations. Final material acceptance requires ten exact document hashes, three-platform OS and size disclosures, physical evidence for keyboard, mouse, and both controller layouts, separate content sizes, exact save locations, six candidate screenshot roles, two candidate video roles, and eight evidence-linked claims. Current pending markers and absent candidate media keep release acceptance false.
- Added the V090-10 `release-rehearsal-handoff-v1` contract and validator. Three platforms each require eleven passing staged acquisition, signature, install, save, optional-content, update, rollback, and removal operations. Candidate and previous artifacts, manifests, release materials, migration fixtures, protected user data, withdrawal, operational authority, and every evidence file are hash-bound. No staged rehearsal record exists, so rehearsal and release acceptance remain false.
- Added the final `stable-promotion-handoff-v1` guard. Tag and version `1.0.0` cannot pass until ten upstream decisions accept one revision, the protected workflow rebuilds it, all public artifacts and their provenance/checksums match, the approved optional pack stays separate, public-file installs pass, the complete release record is preserved, and six compatibility promises remain exact. No promotion record exists, so stable promotion and release acceptance remain false.
- Added `candidate-install-lifecycle-preflight-v1` to Release artifact qualification. The real exported player now exercises first launch, hash-identical repair and relaunch, read-only and non-ASCII install/user paths, all seven known legacy save fixtures, future-schema rejection without rewrite, optional-pack and player-data recovery, and application removal with external data retained. The aggregate matrix requires the preflight on Windows, macOS, and Linux while selected-channel installer lifecycle and real cross-version binary rollback remain open.
- Added candidate-only exported-player launch reliability. Release matrix jobs run 100 clean, timeout-bounded, warning-free launches from a read-only install with distinct fresh profiles on each desktop platform, and the aggregate matrix requires all 300 before provenance. Exact Release run 32421705560 passes and retains all 300 launches across Windows, macOS, and Linux before three successful manifest attestations.
- Added `release-matrix-qualification-v1`, a post-build CI gate that cross-binds Windows x64, macOS Universal, and Linux x64 source revision, build mode, deterministic state hash, dependency lock set, artifact manifest, unsigned signing-readiness, read-only install, and deterministic package evidence. Provenance now depends on the complete matrix. Protected signing, notarization, Linux runtime and desktop integration, final provenance, and channel approval remain open.
- Prepared the inactive V090-01 `candidate-freeze-policy-v1` boundary. CI currently resolves 93 files across rules, save schemas, replay schema, content manifests, input defaults, accessibility defaults, and candidate fault qualification; validates prerequisite, severity, permitted-change, and required-evidence contracts; and can build a deterministic file and contract SHA-256 baseline after all 0.8 acceptance dependencies close. No candidate freeze is currently claimed.
- Completed the V080-11 offline comparison foundation with stable tamper-evident seed codes, four explicit source-preserving household slots, equal-rules isolated ghosts, atomic privacy-safe run cards, exact content-hashed deletion consent, recovery ownership, raw keyboard/controller routes, and live Godot qualification. Household handoff, platform presentation, and ghost readability remain human gates.
- Completed the V080-10 optional lore foundation with a closed 41-entry three-depth offline archive, existing-progress unlocks, raw keyboard/controller browse routes, strict copy and continuity validation, and rules/progression isolation. Canon, tone, humor, platform presentation, and curiosity pacing remain human gates.
- Completed the V080-09 interactive spectator foundation with ten measured rivals, equal-rules lanes, local standings and rivalry history, authored commentary, typed explanations, exact-seed human challenges, raw keyboard/controller routes, persistence recovery, and progression isolation. Physical-controller, platform, editorial, pacing, and entertainment review remain human gates.
- Completed the V080-08 automated capture and sharing foundation. The remappable Help action toggles clean gameplay and replay capture from keyboard or controller, hiding run HUD, replay controls, terminal, audio-status, debug, and spectator overlay families without changing rules state. Verified replay export now also writes a closed 24-field schema-1 run summary with application, rules, mode, score category, config, integrity, seed, and outcome metadata, explicit identity/path exclusions, atomic no-overwrite writes, and idempotence. `capture-sharing-qualification-v1` retains deterministic playback and privacy evidence while platform captures and final composition remain human gates.
- The V080-07 automated localization migration initially established 503 stable shell copy IDs and 66 strict named-parameter templates across thirteen shell flows. The current catalog has expanded to 647 IDs and 99 templates, plus the retained onboarding, feedback, broadcast-caption, optional-lore, offline-comparison, Agent Arena, pseudo-locale, glyph, and 150-percent layout checks. Visible multi-platform review remains a human release gate.
- Added the V080-06 native `ValidateCreatorContent` command with data-only personality and canonical pack-set modes, stable schema-1 JSON reports, closed published schemas and examples, 31 documented validation/compatibility codes, hard duplicate-ID collision rejection, core-then-ordinal optional resolution, and executable no-code/no-network qualification. Arbitrary code plugins remain outside 1.0.
- Added the V080-05 automated content-curation handoff: all 95 radio assets are assigned once across eight balanced station candidate lists, duplicate and suspicious release inputs are rejected, current approvals remain zero, and deterministic manifest-bound human-readable credits and third-party notices generation is available. Full decode, loudness, listening, core-music selection, badge review, production manifests, and export approval remain explicit gates.
- Added the native V080-04 progression foundation: twenty exact three-lane goals, highlighted-goal persistence, eight curated cosmetic sets with exact locked requirements and five saved loadouts, a finite twelve-event/four-tier fixed-seed Broadcast Tour, expression-only earned rewards, safe notification queuing, strict `progression.json`, and raw keyboard/controller qualification. Tour practice records deterministic replays and same-seed rematches while remaining outside competitive scores, ordinary achievements, local playtest summaries, and human-run progression metrics.
- Replay schema 1 captures canonical shell-supplied UTC time plus explicit gameplay and AI seeds while retaining byte-compatible legacy envelopes without those optional fields.
- Pure clock-free replay playback verifies at construction, advances one deterministic rules step at a time, and supports exact reset and forward or backward seek without mutating player progression.
- The bounded replay store lists generated replay summaries newest first without reading payloads, enforces count and byte budgets, and rejects untrusted manual names from the browser.
- Godot now provides a background-verified replay browser and playback screen from `R` or Controller North. Each bounded row shows date, mode, rules version, score, seed, duration in steps, and explicit verified/incompatible/modified/unreadable state without exposing internal names or hashes. Keyboard/controller playback adds 0.5x/1x/2x/4x speed, pause, single-step, back-ten, HUD toggle, restart, and return; focus loss and last-controller disconnect pause playback. Verified export is atomic and bounded. Per-item deletion uses content-hashed two-step consent, rejects stale plans, cancels losslessly, removes exactly one stored replay, and preserves exports. `replay-browser-qualification-v2` proves both raw input families and progression isolation.
- Central shell transition methods now own every post-initialization screen and pause-state write. Structural tests and the real scene smoke lock the exhaustive nine-state transition graph and replay/settings flows.
- Required `input-cadence-qualification-v1` evidence feeds real Godot keyboard, D-pad, and stick events through the live InputMap mapper and production fixed-step drain under low, normal, and stressed render schedules. All nine cases accept and consume the same five-turn stream exactly once, end with no queued input, share one rules hash, and reject passive stick drift.
- Preferences schema 6 introduced default-off local playtest consent when the settings screen had 33 described rows. The current schema 7 and 34-row screen retain that migration, the Vibe adaptation preference, persisted Master-bus mono downmix, 10 to 90 percent shared gameplay-stick deadzone, D-pad fallback, keyboard/controller navigation, resets and recovery, local summary controls, and the complete settings qualification evidence.
- The strict VirtualViewport evidence matrix now includes an explicit classic 4:3 case and rejects missing, duplicate, or extra matrix entries.
- Typed `accessibility-presentation-v1` policy/evidence covers default, reduced-motion, flash-free, and combined profiles. Full-screen flashes are prohibited, shake is zeroed under protective profiles, caption time is never shortened, all 31 cues and critical text remain available, and presentation settings cannot change the rules hash.
- First-run onboarding detects a missing profile decision and offers direct play or eight deterministic, interactive, unscored lessons for turning, reversal rejection, wrapping, food and score, starvation, Shield, pause, and restart. H or controller left-stick replays it; Data resets only tutorial progress; skip/completion/reset persist atomically; active-device prompts and hard score/achievement/replay isolation retain `onboarding-qualification-v1` evidence.
- Run end now presents outcome, exact collision or starvation cause, relevant recovery guidance, score, fair-category personal best, new-record state, run statistics, new unlocks, replay status, and rules/config identity in a stable order. Confirm is the only restart action; the terminal input sequence cannot restart; later keyboard Enter or controller South can; menu, settings, and replay access remain available through `run-end-qualification-v1` evidence.
- Data settings now separate preferences/bindings, progression, personal bests, replays, and optional content. Confirmation lists exact `user://` targets; background reset creates and SHA-256 verifies a bounded backup before removal; cancel writes nothing; quit waits safely; restore never overwrites current data; corrupt/incomplete backups remain visible and non-restorable with their location and recovery choices. `player-data-recovery-qualification-v1` retains keyboard/controller routes and all failure contracts.
- The bare arcade loop now retains explicit one-step input, three-turn buffer, 3:1 graphical contrast, wrap, host-smoke pacing, same-step death attribution, deliberate restart, and zero-residual reset budgets. Production food color now clears 3:1 against both the head and board. Six cross-aspect/accessibility semantic frames plus linked evidence and an explicit pending-human checklist form `bare-arcade-loop-qualification-v1` without claiming physical feel or platform pixels.
- A closed typed feedback matrix now covers all 19 ordered rules events and 15 shell-action families. Every row declares one dominant channel, visual/audio/text/haptic behavior, priority, cooldown, polyphony, stacking, interruption, ducking, shake, flash, hitstop, criticality, accessibility alternatives, implementation state, and authored-asset state. Qualification accounts for all 31 fallbacks and explicitly reports zero approved-but-unused shipped feedback assets.

- Pure `ReleaseArtifactManifest` schema 3 validates native export inspection manifests, including the supported-player preview-exclusion state, platform payload patterns, SHA fields, and byte-sum integrity, and declares installer/archive shapes for Windows/Linux portable folders and macOS app-bundle zip (V030-10).
- `ValidateArtifactManifest` tool and export inspection wire pure C# validation after writing `artifact-manifest.json`.
- Godot bindings browse/remap screen (`B` / right shoulder): Left/Right selects keyboard or controller, Up/Down selects an action, Confirm captures a key, controller button, or deliberate axis, Back cancels, and F8 restores defaults. Remaps persist and reapply without dropping the opposite device.
- Xbox, PlayStation, Nintendo, and generic text prompt families switch only after deliberate input and show the active binding on menu, run-end, achievements, and bindings screens.
- Binding conflicts now identify the owning action and enter an explicit resolution state: Confirm atomically swaps both keyboard or controller actions, while Back/Escape cancels without changing or persisting either binding.
- Central `ShellTheme` palette/font ownership and asset-free vector prompt badges cover keyboard, Xbox, PlayStation, Nintendo, and generic controller labels across menu, run-end, achievements, and bindings flows. Every badge retains readable text.
- Required `shell-presentation-v1` evidence locks two palettes, minimum contrast, all prompt families, all eight badge shapes, centralized font metrics, 150-percent text-layout bounds, scale-aware long-list rows, keyboard/controller achievement pagination, non-color state/focus markers, text fallback, and required-flow coverage.
- Required `virtual-viewport-matrix-v1` evidence covers minimum clamp, 16:9, 16:10, ultrawide, square, 4K, and 5K aspect preservation, pointer round trips, and letterbox exclusion.
- Required `audio-mixing-policy-v2` evidence covers all 31 finite cues, bounded 8-voice SFX and 4-voice UI allocation, cooldown, polyphony, priority, stable interruption, strongest-active music ducking and restoration, immediate isolated saved volumes, output-topology polling and repair, 992 rapid retriggers, full-catalog mute suppression, injected missing-bus failure, bounded retry, recovery, cache bounds, cleanup, and deterministic-rules isolation. The playback-free allocator adds 25 focused contracts.
- `sfx-catalog-qualification-v1` proves distinct navigation/confirm/back, restart, achievement, four combo tiers, combo break, both death causes, and all nine power activations. All 31 procedural PCM fingerprints are unique, licensed, provenance-declared, stereo 22.05 kHz, measured inside the -24.5 to -18.0 dBFS peak window, and connected through the feedback matrix. Authored SFX remain explicitly unapproved pending rights, -18 LUFS/-1 dBTP normalization, decode, repetition, and listening review; candidate metadata and blocked inventory assets cannot enter native artifacts.
- Production `multimodal-feedback-v1` presentation now gives starvation four named color roles plus exact time and segmented shapes, moves score and combo together while retaining a reduced-motion marker and readable multiplier, assigns every power a stable icon/name/state/effect cue, telegraphs Shield, Phase Shift, Last Stand, and recovery protection, and distinguishes collision from starvation with exact text and stable `[X]`/`[0]` geometry. Default, muted, reduced-motion, flash-free, and combined minimum-effects qualification retains at least two death-attribution channels without changing rules state.
- Native `RadioCatalog`, `RadioPlaybackPolicy`, and `RadioStreamPlayer` now project only validated manifest metadata, load one hash-verified MP3 at a time, define shuffle/no-repeat/single-track-repeat/pause/resume/station-switch/end behavior, retain per-station track identity, isolate missing tracks and packs, and consume only the named radio random stream. Bounded station/track/pack/mute/help UI appears on menu, run, and Content Packs; `J` and controller `R3` cycle stations. Twelve focused contracts and `radio-behavior-qualification-v1` retain packaged-inventory, decoder, input, fallback, rules-isolation, and gameplay-RNG-isolation evidence while production packs remain unapproved.
- Production `VisualHierarchyPolicy` now caps particles, per-event emission, shake sources and strength, full-screen flashes, popups, overlays, head-effect outlines, and popup text. Peak feedback is limited to death prevention, death, major achievement or grid completion, and maximum combo. The live renderer uses pressure-aware caption color, bounded caption text, protection-first outline selection, a permanent head-direction marker, and policy-owned terminal opacity. `visual-hierarchy-qualification-v1` measures standard and high-contrast palettes and writes five hash-verified PNG review frames for quiet, busy, warning, recovery, and game-over states without advancing rules.
- `performance-qualification-v1` now measures 40 live Godot frames each for minimum, default, and maximum-safe effects. The maximum mixed scene fills the 2,112-cell board with 2,107 snake cells, three hazards, food, and a power signal while drawing 160 particles and three popups. CI enforces 12 audio channels, at most 2,400 logical draw submissions, gross shared-host p95/max frame ceilings, and an identical final rules hash after 256 steps per profile. The 60 FPS target, driver draw calls, allocation, memory, thermals, and long-session acceptance remain named-hardware gates.
- `VibeLevelDirector` now exclusively maps combo 0/3/5/10/20 to Grounded, Flow, Heat, Overdrive, and Transcendent. Combo feedback, stingers, visual priority, board palette, HUD role, and bounded trail rendering consume typed director output instead of repeating thresholds. Five level definitions declare background/HUD/trail/particle/camera/music/stinger/accessibility budgets, transitions fire once, critical gameplay stays dominant, and every palette remains above 4.56:1 gameplay contrast. `vibe-level-qualification-v1` gates 13 fixed scenes and seven accessibility profiles with unchanged rules hashes and score categories.
- Hosted Windows, macOS, and Linux Godot jobs retain every native qualification JSON for 14 days, including viewport, audio, presentation-frame, throughput, property-campaign, and content-eligibility evidence.
- Lock-derived `dependency-inventory-v1` evidence records unique NuGet and Python packages, every source-lock SHA-256, a combined lock-set digest, pinned Godot/.NET tools, runtime identifier, full Git revision, and dirty-state provenance. Native qualification validates and retains it per platform.
- Export qualification stages the install, user profile, and log under paths with spaces and non-ASCII characters, makes the installed player temporarily read-only, proves a real adjacent write is rejected, launches from a fresh profile with writable data and logs outside the install, verifies every installed file is unchanged, and retains `artifact-read-only-install-v1` evidence on Windows, macOS, and Linux CI.
- Pure C# content-pack schema 1 parsing enforces exact fields, duplicate-field rejection, bounded UTF-8 documents, canonical encoding, safe paths, semantic ranges, dependencies, complete inventory allowlists, file metadata, cleared rights/credits, and radio station contracts.
- Native pack-set resolution treats the core as mandatory while independently isolating absent, removed, malformed, incompatible, tampered, and duplicate optional packs. Godot retains `core-only-offline-v1` evidence across launch, menu, run, critical feedback, settings, content-pack browse, death, restart, and recovery.
- Immutable optional-pack removal consent protects core, requires a current installed-pack version, makes cancel lossless, removes only the selected optional pack on confirm, and has no save/profile/replay deletion capability. The Godot `C` / west-button content screen publishes that separation to keyboard and controller players.
- `OptionalPackStore` validates canonical installed manifests, exact file allowlists, sizes, SHA-256 payloads, safe roots, entry bounds, and link rejection. Confirmed removal uses a same-volume move into recoverable quarantine, restore revalidates content before moving it back, and packaged Godot smoke proves unrelated player data stays unchanged.
- Installed optional assets are exposed only as bounded bytes and media metadata after complete pack validation plus a second size/hash check. Quarantine entries are rediscoverable after restart, and tampered quarantine remains recoverable in place but cannot be restored.
- Strict `release-signing-policy-v1` keeps signing material out of source, artifacts, and ordinary CI; locks Authenticode, Developer ID/hardened-runtime/notarization, and Linux provenance routes; and emits artifact-manifest-linked `release-signing-readiness-v1` evidence. Debug builds are explicitly not promotable.
- Tag and manual release qualification attest artifact manifests through a separate least-privilege GitHub OIDC/Sigstore job. Detached provenance bundles and qualified players retain for 30 days; actual platform signing remains a protected release operation.
- `ReleaseOutputPlan` defines exact Windows ZIP, macOS app-bundle ZIP, and Linux tar.gz direct-download outputs plus portable/app-bundle store-depot shapes. Qualification packaging rehashes the exact manifest allowlist, repeats package creation byte-for-byte, emits separate SHA-256 checksums, keeps optional packs/player data separate, preserves uninstall data, and remains explicitly non-publishable.
- Pure `AudioOutputRecoveryTracker` deduplicates unavailable/restored transitions, sanitizes diagnostic reasons, and applies monotonic bounded retry timing without referencing Godot or rules state.
- Dual-runtime `achievement_candidates_rules_v1` fixture and parity suite with the product flag enabled (score candidates, already-unlocked suppression, empty zero-score emission) without flipping default-off core/power corpora (PD-009).
- Pure `AchievementsBrowseReport` projects the rules-local catalog against permanent unlocks (summary, rarity progress, filtered entries, preview) and drives menu and ended-run unlock captions.
- Shell achievements browse screen (`U` / left shoulder) lists the full rules-local catalog with unlock markers; smoke covers open/return transitions and `achievements_browse_open`.
- Multi-power synergy and anti-synergy campaign tests for the nine-power portfolio (protection handoffs, cross-family composition, same-kind anti-stack, restore, restart cleanup).
- Architecture boundary tests ban Console.Write from pure Rules sources.
- Architecture boundary tests ban Task.Delay from pure Rules sources.
- Architecture boundary tests ban Thread.Sleep from pure Rules sources.
- Architecture boundary tests ban Process.Start and GetCurrentProcess from pure Rules sources.
- Achievements maximum unlock capacity is locked at 256 and verified above catalog size.
- Main menu previews the first unlocked achievement IDs when any permanent unlocks exist.
- Achievements documents reject empty-string and null unlockedIds entries.
- Architecture boundary tests ban wall-clock, global random, and env access from Persistence sources.
- Python CoreSimulation accepts already_unlocked_achievements to suppress known profile unlocks during dual-runtime experiments.
- AchievementsDocument.IsUnlocked helper for profile unlock queries.
- Ended-run overlay shows RUN UNLOCKS progress from permanent profile unlocks.
- Achievements documents accept schema_version as an alias for schemaVersion on load.
- Headless smoke asserts structured `achievements_load` after shell startup unlock restore.
- Ended-run overlay can show UNLOCK SAVED feedback when permanent unlocks are written.
- Architecture boundary test locks Rules→Persistence one-way dependency (no cycles with Game).

- Main menu shows rules-local run unlock count (`RUN UNLOCKS n/total`) from `achievements.json`.
- Inventory gate writes `content-eligibility-evidence-v1` JSON under `TestResults/native` for pack-approval handoffs.
- Achievements document golden serialization and absolute user-data root rejection coverage.

- Pure `ContentEligibilityReport` summarizes inventory ship, rights, and media-type eligibility breakdowns for pack-approval handoffs while exportEligible remains zero.

- Godot shell loads and saves permanent achievement unlocks; SnakeRun.ApplyProfileUnlocks suppresses already-owned candidates without affecting scores or state hashes.
- Restart clears session achievement counters so the next run cannot inherit unlock metrics.
- Death-cause contract fixtures for SelfCollision, Starvation, and closed None set; state-machine campaigns assert session-counter restore parity.
- Pure `AchievementsDocument` schema 1 and atomic `AchievementsStore` for permanent catalog unlock IDs under `achievements.json` (profile unlock foundation).
- Architecture boundary tests ban filesystem I/O surfaces from pure `VibeSnake.Rules` sources (V040-09).
- Versioned pure-rules property campaign report producer (`rules-property-campaign-v1`) writing `TestResults/native/property_campaign.json` with seeds, invariants, failure payload, and one-command reproduction (V040-10).
- Canonical state schema 3 and `fnv1a64-canonical-json-v4` include session achievement counters (`sessionFoodEaten`, `sessionWraps`, `sessionNearMisses`, `sessionPowerupsCollected`, `sessionMaxCombo`) so mid-run restore preserves unlock eligibility (PD-010). Schema 2 states remain intact and fail compatibility.
- Pure `RunConfig.ComputeConfigHash` / `SerializeCanonicalConfig` (`sha256-canonical-runconfig-v1`) and `SnakeRun.ConfigHash` for score and replay metadata without altering the step state hash.
- Pure `InputBindingsDocument.TryRemapAction` for conflict-safe single-action remapping without mutating the source document.
- Pure `InputBindingsDocument.TrySwapActions` for atomic two-action binding exchange without intermediate conflicts.
- Shell accessibility hotkeys: F7 master mute, F9 high contrast, F10 reduced motion, F11 fullscreen, each persisting preferences; `ShellSettings` toggle helpers and headless smoke coverage.
- Pure `ControllerConnectionTracker` with sanitized connect/disconnect captions; shell seeds joypads, shows menu notices, and pauses a run when the last controller disconnects.
- Optional `configHash` / `configHashAlgorithm` fields on offline crash reports; rules throughput evidence JSON records the effective config hash.
- Pure `RunScoreIdentity` for fair-score categories using ruleset contract plus effective config hash.

- Master volume step hotkeys (`=` / `-` and keypad equivalents) with clamp, unmute-on-raise, and preference persistence.
- Ended-run overlay shows a compact `RunScoreIdentity` support caption (ruleset contract, score, config-hash prefix).
- Text scale step hotkeys (F5 down, F6 up) with preferences schema clamp (0.85..1.5) and persistence.
- Flash-free toggle (F4) and open-diagnostics action (F12) for the offline support path; F12 also copies the diagnostics absolute path to the clipboard.
- Dual-runtime `combo_expired` event: Python `CoreSimulation` emits it in native order; `EnableComboExpiredEvent` defaults true; regenerated `core_rules_v4` (35 cases) and golden state hash.
- Dual-runtime near-miss scoring: `CoreSimulation` applies clutch/body near-miss through the production detector; `EnableNearMiss` defaults true; `core_rules_v4` includes clutch on deadline food rescue.
- Pure `AchievementCatalog` candidate evaluation for run-local metrics; `SnakeRun` session counters for food, wraps, max combo, and power collections; expanded personality document validation coverage.
- Terminal `RunEventKind.AchievementCandidate` emission gated by `RunConfig.EnableAchievementCandidates` (default false for shared-fixture parity; product runs enable true); shell ACHIEVEMENT captions via catalog index; `IndexOf` / `DefinitionAt` helpers.
- Replay envelopes store `configHash` / `configHashAlgorithm`; verification rejects config identity drift (`ConfigIdentityDiverged`); unsupported config-hash algorithms fail closed on read.
- Offline `StructuredLocalLog` JSONL writer with `DiagnosticLogLevel` filter, path sanitization, 1 MiB rotation, and shell hooks for session start, diagnostics open, preferences faults, and controller connect/disconnect.
- Python dual-runtime `qa.achievement_candidates` catalog matching native IDs/order; `CoreSimulation` optional `enable_achievement_candidates` (default false) emits terminal `achievement_candidate` events.
- Godot headless smoke asserts structured session log contains `smoke_session_start` and `open_diagnostics` event codes.
- `RunStepResult` equality/operator coverage; README screenshot presentation-source fingerprint refresh after dual-runtime QA source changes.
- Flash-free presentation softens high-intensity captions, lengthens caption dwell, and skips non-critical audio cues while keeping death/victory/pause/confirm.
- Enabling reduced motion zeros screen-shake intensity so the preference cannot leave residual shake.
- `ShellSettings.EffectiveScreenShakeIntensity` forces zero under reduced motion or flash-free for future camera effects.

### Fixed

- Release exports no longer require checkout-only `Main.cs` localization inspection or the authoring-only power-decision JSON during packaged smoke. Source migration and contract audits remain mandatory in the editor qualification, while the packaged player independently verifies its closed localization catalog, pseudo-locale layout, and live feature paths.
- Flash-free no longer suppresses unrelated audio cues, and reduced motion no longer shortens the player's caption reading window.
- Input binding documents reject unsupported device classes, cross-device tokens, unknown adapter tokens, and physical-axis conflicts before save or InputMap application.
- Controller remaps suppress overlapping secondary stick fallbacks, and capture ignores low-amplitude axis drift.
- Terminal `AchievementCandidate` events emit at most once per run; restored terminal states do not re-fire on idle `Step()`.
- Architecture boundary tests also ban HTTP client and System.Net.Http/Sockets from Rules sources and Persistence assembly references.
- Generated state-machine campaign covering once-only `AchievementCandidate` emission across terminal idle steps and terminal restore.
- Shell `WriteLocalCrashReport` helper pairs structured Error log lines with offline crash reports; smoke asserts `smoke_crash_probe`.
- Controller input-binding load faults write the same offline crash report and structured Error path as keyboard load faults.
- Replay envelopes accept optional shell-supplied `appVersion` on capture (product and smoke recorders pass `ProductIdentity.AppVersion`); legacy envelopes without the field remain readable; smoke asserts stored replays retain the version.
- `ProductIdentity.AppVersion` centralizes the shell product version for crash reports and replays; Godot `*.cs.uid` companions are gitignored.
- Shell structured log records `replay_finalized` and `replay_finalize_failed` around terminal replay capture.
- Shell structured log records `run_start` when a product run is created or restarted, and `run_won` / `run_dead` when it ends; smoke death path mirror-completes the terminal step, saves synchronously, and asserts `run_dead`, `replay_finalized`, and post-restart `run_start`.
- Native and exported player smoke harnesses accept 1-4 isolated replays so storage smoke and death-restart can each leave a verified envelope.
- Mid-run restore preserves session achievement counters under canonical state schema 3 (replaces the prior schema 2 characterization gap).
- CI gates `ProductIdentity.AppVersion` against `pyproject.toml` package version via `scripts/check_product_version.py`.
- README screenshot presentation-source fingerprint refreshed after dual-runtime achievement-candidate QA wiring.
- `InputBindingsDocument.TryRemapAction` preserves fractional axis thresholds instead of rounding them to integers.
- Multi-stream `RandomStreamBank` for independent gameplay, AI, cosmetic, radio, and copy PCG32 streams derived from one master seed.
- Fail-closed native custom AI `PersonalityDocument` validation with schema, trait range, boolean rejection, RGB checks, and filename-scoped errors.
- Architecture boundary tests that forbid Godot/presentation references from `VibeSnake.Rules` and `VibeSnake.Persistence`.
- Declared `ContentPackBudgets` ceilings for core and radio pack size and timing gates.
- Godot automated menu to run to death to restart smoke on a forced self-collision terminal path with replay save drain.
- Offline `LocalDiagnostics` crash reports with path sanitization, retention limits, and no network submission.
- In-memory `ShellSettings` multi-bus volumes and reduced-motion / high-contrast / flash-free placeholders for the 0.5 accessible shell.
- Explicit `ShellTransitions` table for legal menu/run/pause/ended presentation transitions.
- Preferences schema 2 with multi-bus audio and accessibility fields, schema 1 migration, and atomic `PreferencesStore`.
- `SnakeRun.Create` records master seed via `RandomStreamBank.Gameplay` without changing scored RNG outcomes.
- Headless smoke writes host-dependent `presentation_frames.json` p50/p95/p99 evidence under TestResults/native.
- Schema 1 logical `InputBindingsDocument` with conflict detection, required escape-hatch actions, and atomic per-device-class storage.
- Pure `InputBindingToken` parser for `key:`, `button:`, and `axis:` tokens with unit coverage.
- Godot `GameActions.ApplyKeyboardBindings` / `ApplyControllerBindings` apply stored documents to the InputMap while preserving the opposite device class, secondary convenience keys, and stick axes; headless smoke remaps pause and restores defaults.
- Logical `VirtualViewport` 1280x720 letterbox and pointer transform contract for shell scaling.
- Godot shell draws through `VirtualViewport` letterbox transforms (engine stretch disabled), tracks window resize, and maps window pointers into logical canvas space.
- Pure `NearMissDetector` for body-proximity tiers, edge ride, clutch eat, style points, and bounded combo windows using fixed rules ticks.
- `ContentBudgetReport` inventory totals vs declared pack ceilings (including core working-set membership), plus ContentService packaging resolve codes and media-type listing without loading media.
- `ContentInventory.CountByMediaTypePrefix` (and ContentService delegation) for pack composition queries.
- Complete `ContentPackBudgets` predicate helpers for core working-set, radio station compressed/installed ceilings, inventory-scan timing, and cold-start timing.
- `ContentTimingReport` for measured inventory-scan and cold-start timings against declared ceilings (no declared-hardware claim).
- Shell settings apply multi-bus volume and mute to the Godot Master/Music/SFX/UI buses; high-contrast canvas colors, text scale, and shortened reduced-motion feedback captions.
- `SnakeRun` can award near-miss score events for body proximity, clutch eats, and boost style points via pure `NearMissDetector` when `RunConfig.EnableNearMiss` is true (default false until shared fixtures regenerate); shell feedback recognizes `RunEventKind.NearMiss`.
- Interactive Godot sessions apply preferred fullscreen mode from shell settings (headless smoke stays windowed).
- Escape-hatch restore-defaults input action (F8 / controller select) rewrites keyboard and controller binding documents and re-applies the InputMap.
- Closed `RulesEventCatalog` of ordered `RunEventKind` wire names for deterministic event publication (includes `near_miss` and `starvation_warning`).
- `RunEventKind.StarvationWarning` / `RunEvent.StarvationWarning` emit once when hunger crosses the configured warning band (default 200 ticks remaining).
- Godot shell captions and orange HUD tint for the starvation warning band, with headless smoke coverage.
- Reserved `RunEventKind.ComboExpired` / `combo_expired` catalog entry for upcoming dual-runtime fixture regeneration (state reset remains silent for parity).
- Reserved `RunEventKind.AchievementCandidate` / `achievement_candidate` catalog entry for progression wiring without emission yet.
- `LocalDiagnostics.EnsureDiagnosticsDirectory` and interactive shell `OpenDiagnosticsDirectory` for in-game open-folder support without network paths.
- `RulesEventCatalog.PresentationPriority` and `SelectPrimaryKind` for caption selection when multiple events share a step.
- Native pure C# Phase Shift power contract with collection, timed expiry, body-overlap movement, Shield precedence, canonical restore, and replay participation.
- Six shared Python-to-C# Phase Shift parity fixtures (`phase_shift_rules_v1.json`) and CI check via `python -m vibesnake.qa.shared_phase_shift_traces --check`.
- Native pure C# Last Stand power contract: held revive, half-body shrink, hunger reset, recovery immunity, collision precedence with Phase Shift and Shield.
- Five shared Python-to-C# Last Stand parity fixtures (`last_stand_rules_v1.json`) and CI check via `python -m vibesnake.qa.shared_last_stand_traces --check`.
- [Migration ownership map](docs/engineering/MIGRATION_MAP.md) for V030-12.
- Native Slow-Mo and Boost cadence modifiers with composable snapshot scale and fixed-step movement invariance.
- Native Magnet food attraction (one-cell pull toward the head each rules step, blocked by body and pickups).
- Native Bait (weighted next-food respawn), Gluttony (eat without growth), and Segment Detach (timed tail obstacles with Phase Shift bypass).
- Pure `RulesCadenceClock` for Slow-Mo/Boost wall-clock step intervals with re-evaluated tempo during multi-step drains.
- Godot presentation for the full nine-power portfolio: letter markers, composite HUD, head outlines, body tints, bait marks, detached hazards, multi-power captions, and generic power fallback cues alongside Shield-specific tones.
- Nine shared Python-to-C# remaining-power parity fixtures (`remaining_powers_rules_v1.json`) covering Slow-Mo, Boost, Magnet, Bait, Gluttony, and Segment Detach, checked via `python -m vibesnake.qa.shared_remaining_power_traces --check`.
- Automated parity delta reduction (`ParityDeltaReducer`) that minimizes failing command prefixes, proves clean re-execution, and records minimized reproducers on movement divergence bundles.
- Host-dependent pure rules throughput evidence (`rules_throughput.json`) with a conservative CI floor for the technology decision gate (presentation frame times remain unmeasured).
- Expanded [migration map](docs/engineering/MIGRATION_MAP.md) with save, replay, pack, and ruleset data-migration procedures plus a dual-runtime freeze checklist.
- Published [user-data directory contracts](docs/engineering/USER_DATA.md) for Python and native platform roots, layouts, recovery, and separation rules.
- Python QA contracts for the remaining-power shared fixture (`test_shared_remaining_power_traces.py`).
- Native content-inventory gate that keeps `exportEligible` at zero until pack approval, and rejects rooted or traversing inventory paths.
- Pure `ContentInventory` reader in Persistence for allowlist queries, with path-traversal rejection and public inventory parse coverage.
- Native artifact inspection refuses inventory path traversal and blocks packaging of assets that are not `exportEligible`, with shared PowerShell policy coverage.
- Godot `ContentService` smoke validates allowlist denial with an embedded fixture so packaged-player smoke outside the checkout does not require development inventory files.

### Changed

- Native `test_native.ps1` retries Coverlet once after a rebuild when hit-file truncation fails on Windows after a green test run.
- Documentation snapshot refreshed for public radio inventory, hosted multi-platform CI, and single-`main` repository hygiene.
- Canonical run state serializes phase-shift and last-stand fields.
- Godot shell advances rules through cadence-aware accumulation instead of one physics frame per rules step.
- Native power collection now runs after movement settles so Segment Detach matches the Python coordinator body state.

## 0.2.1 - 2026-08-02

### Added

- Player CLI commands `vibesnake play`, `update`, `status`, `doctor`, and `version`, plus `play.ps1` / `play.sh` / `play.bat` launch helpers.
- Continuous [player-latest](https://github.com/blisspixel/VibeSnake/releases/tag/player-latest) GitHub release rebuilt from every `main` push with playable source zip, wheels, and checksums.
- Player-facing install scripts (`install_player.ps1` / `install_player.sh`) and player-build workflow.
- Adaptive window presentation with preferred 4:3 framing, integer pixel scaling, and retro-modern title/settings/pause chrome.
- Preferred Snakev2 brand logo hash gate and README captures for main menu, customization, and powers-active play.
- GTA-style offline radio library (95 tracks, eight stations) shipping in public source under `assets/audio/radio/`.

### Changed

- Default player documentation centers on `vibesnake` commands and GitHub `main` updates.
- Dependabot version-update PRs disabled (`open-pull-requests-limit: 0`) so the public repository stays on a single clean `main` branch.

## 0.2.0

### Added

- Canonical documentation hub, status report, roadmap, player guide, architecture guide, subsystem references, and release checklist.
- Detailed capability-gated release plan from 0.3.0 through 1.0.0, including product scope, compatibility policy, acceptance gates, quality growth, risk controls, and primary research references.
- GitHub Actions quality workflow for Python 3.11 through 3.14.
- Pytest, coverage, and Ruff configuration in `pyproject.toml`.
- Isolated test save directories through `VIBESNAKE_DATA_DIR`.
- Headless rendering, input, persistence, achievement, and entry-point tests.
- Main-loop integration tests for all nine power-up contracts.
- Versioned user preferences for sound, volume, and fullscreen mode.
- Configuration, save-path, migration, corruption, and atomic-write tests.
- Apache License 2.0 terms, NOTICE boundaries, contribution, conduct, security,
  support, pull-request, and dependency-update policy, with public intake closed
  until confidential reporting routes are tested.
- Seeded reference-core QA runner with food-seeking, survival, and abusive-input policies.
- Per-step gameplay invariants, action traces, immediate replay hashes, JSON campaign reports, and a CI-friendly QA command.
- Property-based generated command-sequence tests through Hypothesis.
- Canonical fun and player-experience, automated QA, and native technology strategy documents.
- Godot 4.7.1 C# qualification project with menu, logical keyboard and controller actions, focus-loss pause safety, quit and back flows, run, death, restart, drawing, and a deterministic headless smoke mode.
- Pure `VibeSnake.Rules` assembly with PCG32 randomness, bounded commands, wraparound movement, food, growth, combo scoring, exact starvation, collision, grid completion, immutable ordered events, explicit restart, snapshots, strict canonical JSON restoration, and versioned state hashes.
- One hundred seventy-seven xUnit native contracts. Rules coverage is 91.73 percent line and 87.77 percent branch; persistence coverage is 90.73 percent line and 84.48 percent branch; aggregate native coverage is 91.55 percent line, 87.26 percent branch, and 97.53 percent method, with an enforced 80 percent per-module line floor.
- Generated native state-machine campaigns that repeatedly serialize, restore, continue, terminate, and restart deterministic runs under valid and abusive command sequences.
- Native Music, SFX, and UI buses with finite cached 16-bit stereo PCM fallback cues for confirm, back, pause, food, Shield spawn, Shield activation, Shield expiry, Shield break, death, and victory.
- Schema 1 first-divergence bundles that retain the shortest executed failing prefix, expected and actual normalized state and events, native canonical state and hash, fixture identity, environment identity, and exact filtered reproduction.
- Explicit `vibesnake-core@4` ruleset identity, canonical state schema 2, `fnv1a64-canonical-json-v3` hashes, and a schema 1 replay envelope with canonical serialization, logical action attempts, checkpoints, final outcome, compatibility diagnostics, deterministic verification, and SHA-256 payload integrity.
- Live native replay recording that preserves rejected logical attempts, compares every completed step with a private deterministic mirror, compares final canonical state, and fails closed on divergence, lifecycle misuse, command, step, or serialized-size limits.
- Platform-neutral replay persistence with strict UTF-8 and size validation, source-preserving external inspection, precise compatibility and verification results, idempotent payload matching, cross-process transaction locking, no-overwrite same-directory atomic writes, and explicit 256-file and 256-MiB storage limits.
- Legacy-save migration failures now remain on the operating-system user-data path and emit a warning instead of silently reactivating checkout-local or install-local storage.
- Relative `XDG_DATA_HOME` and `LOCALAPPDATA` values are ignored so normal runs cannot redirect player data into the launch directory or source checkout.
- Relative `XDG_DATA_HOME` values are also ignored by the export-template installer, preventing implicit writes beneath its launch directory.
- Deterministic replay-verification work accounting for body hashes and potential full-grid food and power-spawn scans, early adversarial workload rejection, fixed untrusted compatibility diagnostics, and bounded sanitized player captions.
- Godot terminal-run replay capture, background post-write reload and verification, lossless save queuing behind inspection, run-start gating, save-aware quit and window-close handling with a monotonic five-second deadline, single-flight latest-replay input, read-only dropped-file inspection, actionable compatibility captions, and isolated user-data smoke.
- Matching `vibesnake-core@4` identity and explicit injected-or-normalized randomness-policy declarations in all three generated Python-to-C# parity corpora.
- Strict source-content policy plus a deterministic 18-file clean-clone inventory with logical IDs, media types, exact sizes, SHA-256 hashes, bounded structural integrity, duplicate links, pack intent, rights status, and export eligibility. Rights-unverified audio, historical production records, and copied research are isolated in the ignored local archive.
- Content inventory tests and a CI gate that reject unclassified, ambiguous, stale, unsafe, malformed, or silently changed assets.
- Schema 1 core and radio pack validation with canonical manifests, exact approved-inventory allowlists, rights-derived credits, compatibility and dependency ranges, station track contracts, a qualification command, and optional failure isolation.
- One hundred compact Python-generated movement fixtures with 25,600 matching C# steps and per-command queue outcomes, 35 targeted cross-language core-rule cases spanning every current score boundary, queue overflow, normalized random respawns, and terminal rules, plus 8 targeted Shield lifecycle cases including collision recovery at the starvation deadline.
- A complete native Shield contract with deterministic legal spawn, collection on entry, visible and active expiry, anti-stacking, one-use self-collision recovery, starvation bypass, saturated-board discard, restart cleanup, strict restoration, replay verification, and typed ordered events.
- Grid-safe Shield pickup and active-state presentation with visible countdowns, persistent text feedback, active head outline, and prioritized procedural cues.
- A native-runtime architecture decision and an explicit Python-to-C# parity decision log.
- Exact .NET 10.0.302 and Godot 4.7.1 toolchain manifests, stable-only SDK resolution, locked NuGet dependencies, and checksum-verified cross-platform Godot bootstrap.
- Exact Godot 4.7.1 .NET export-template checksum, three desktop export presets, selective template installer, and Godot-required application solution.
- Outside-checkout native player qualification with deterministic smoke logging, export-warning rejection, required Rules, Persistence, and Game payload checks, prohibited-content and project-payload path checks, macOS ZIP inspection, per-file SHA-256 inventory, and a machine-readable schema 2 artifact manifest that records its checksum-bound editor provenance.
- Two clean Shield-enabled Windows x64 debug exports with identical state hash `643077d90db75e8c`, 196-file and 189,537,416-byte inventories, and manifest SHA-256 `309bc5e0c37dd8adf0c24097542aca59b1699c1f5c3930117557875175ab47be`.
- Two clean replay-enabled Windows x64 debug exports with identical state hash `643077d90db75e8c`, 198-file and 189,615,786-byte inventories, isolated verified replay output, and checksum-bound schema 2 manifest SHA-256 `bae7d6369d61c6a57f2fe295f0308c238acc6ccd1e057c20abffc880e8c2ae74`.
- PowerShell qualification regressions that reject editor binaries which only spoof the pinned version text, versioned CPython runtimes, Python frameworks, shared Python libraries, and `.env` variants in player artifacts.
- Windows, macOS, and Linux CI matrices for native rules, real Godot headless smoke, checksum-verified exports, packaged-player smoke, artifact inspection, and retained native bundles.
- A universal 51-package Python 3.11 through 3.14 dependency lock with exact SHA-256 hashes, ordered input digest, atomic regeneration, local verification, and CI enforcement.
- Repository-owned local pre-commit hooks plus executable gates for unfinished-work markers, Python placeholders, bare exception clauses, forbidden emoji, and forbidden em dash across active source and canonical documentation.
- Native import limits for grid dimensions, total cells, canonical state, body and queue counts, replay size, step count, commands, and checkpoints.
- Deterministic README captures for the main menu, Vibe run, and AI spectator
  mode, plus exact source-fingerprint and PNG verification in local hooks and CI.
- Eight deterministic station badges generated with project-owned pixel glyphs and
  checked byte for byte in CI.
- Preferred handcrafted Vibe Snake brand logo retained from the Snakev2 mark and
  hash-checked in CI.
- Strict audits for both hash-locked Python graphs, with no known vulnerabilities
  in the final CI or player-runtime dependency sets.

### Changed

- Combo expiry now clears the streak without erasing elapsed time, preventing a late food from receiving a false speed bonus immediately after expiry.
- Python and native scores now share a 2,000,000,000-point saturation contract, and score events report only points actually awarded at the ceiling.
- The scored behavior correction introduced `vibesnake-core@3`; rules version 2 replays remain intact and are rejected as incompatible instead of being silently interpreted with corrected timing semantics.
- Immediate Shield collection and replay-relevant power state advance the current contract to `vibesnake-core@4`; version 3 state and replay data remain intact and incompatible rather than being silently reinterpreted.
- The source reference now supports Python 3.11 through 3.14 and uses Pygame Community Edition 2.5.7 or newer within major version 2, replacing the nearly end-of-life Python 3.10 floor and legacy Pygame distribution.
- The Python quality matrix now passes 466 tests and reports 3 expected optional-radio skips across all four supported versions, with 87.16 percent line coverage measured on Python 3.14.
- Starvation now advances on every non-food rules step, including a blocked collision. An unrecovered collision remains the attributed cause at a simultaneous deadline, while a Shield recovery consumes the Shield and then resolves starvation without deferring it.
- Native Shield duration configuration now requires at least two ticks, guaranteeing that a collected Shield can protect the first post-collection step under the documented expiry ordering.
- Content-pack test manifests now derive their current half-open rules range from the production rules identity, preventing a rules bump from making the valid offline core stale.
- Shared targeted rules now record whether the gameplay random stream advanced and whether off-path food remained byte-for-byte stable, while allowing each runtime to choose a different legal free respawn cell.

- Radio discovery now recognizes current and legacy filename prefixes and assigns all 95 remaining candidates exactly once.
- Achievement evaluation now matches all 25 current definitions.
- Achievement unlocks now persist with the player profile.
- Human progression is no longer advanced by AI spectator runs.
- Starvation and collision deaths use the same player-run finalization path.
- Superseded reports and plans were moved into the ignored, recoverable local archive so stale and unreviewed records are absent from public documentation.
- Shield now absorbs exactly one collision within its active window.
- Phase Shift, Gluttony, Bait, Last Stand, and Segment Detach now alter normal runs as documented.
- Achievement icons now use portable text badges.
- One `HighScoreTable` now owns the top-ten leaderboard, and the HUD reads that source.
- Legacy single-score data is imported exactly once into the canonical leaderboard.
- Player data now uses operating-system user-data directories with non-destructive checkout migration.
- Profile, cosmetic, leaderboard, and preference documents now use schema version 1, atomic replacement, corrupt-file backups, and future-schema write guards.
- Runtime configuration now validates supported types and ranges, applies resolution presets, and controls power-up visibility duration.
- The 1.0 roadmap now targets Godot 4 .NET with a pure C# rules assembly, step-level parity against the Python reference, and first-class Windows, macOS, and Linux artifacts.
- The roadmap now ties powers, effects, progression, radio, AI spectators, customization, lore, and offline comparison to one explicit fun thesis and human validation gate.
- The active technology pin moved from the provisional Godot 4.6 line to verified Godot 4.7.1 with .NET SDK 10.0.302.
- The QA campaign report moved to schema version 2 with ordered events, explicit win state, and outcome aggregates.
- Shared targeted rules moved to schema version 2 and now compare exact starvation, collision precedence, full-grid victory, and ordered event detail.
- CI installs the exact hash-locked Python graph, audits the resolved lock, lints the complete active Python tree, enforces source policy, and rejects stale dependency inputs.
- NuGet audit now evaluates direct and transitive dependencies during locked restore, with every vulnerability severity promoted to a build failure.
- Settings now create renderer-owned fonts for each Pygame lifecycle instead of retaining invalid import-time font objects.
- Ruff formatting is now an explicit local-hook, CI, contributor, development,
  testing, and release-checklist gate alongside linting.
- Credentialed and mutating legacy audio-production programs, their unused
  dependency profile, historical rename records, and rights-unresolved media are
  isolated in the ignored archive. Public tooling is now deterministic except for
  one explicitly manual local radio-preview command.
- Unsupported player-psychology claims in telemetry and presentation comments
  now describe observable state, timing, and rule contracts instead.
- The unwired Python adaptive-difficulty controller and its unvalidated aggregate
  were removed. Adaptive rules remain a future versioned, disclosed, separately
  scored roadmap capability.
- Test cases now begin from an isolated reference-random stream, making exercised
  branches and coverage evidence repeat exactly across consecutive runs.
- Public source content is reduced to 18 classified, rights-cleared files totaling
  95,377 bytes; none is silently export approved.
- The development graph no longer carries an unconfigured type checker whose
  findings were not enforced; the typed production target remains the warnings-as-errors
  C# rules kernel.

### Fixed

- Strict native state and replay import now reject an impossible self-collision death that retains an active Shield.

- Default pytest collection no longer scans archived or paid-API scripts.
- Stale tests now reflect current movement, scoring, and menu contracts.
- Escape closes the help screen.
- Locked customization requirements no longer compare incompatible values.
- Duplicate unlock-map keys and several lint errors were removed.
- Expired uncollected power-ups are removed, transient effects clear on reset, and duplicate active effect types no longer spawn.
- Direction buffering is bounded and rejects stale queued reversals before they can create delayed input or unbounded queue growth.
- Food collection, growth, scoring, and feedback now occur on entry into the food cell instead of one movement tick later.
- Radio playback now skips unreadable MP3s, remembers failed files for the session, and tries the remaining station tracks before turning the station off.
- Godot export no longer fails silently for lack of the required application solution.
- Native project symbols map source documents to a deterministic virtual root instead of leaking the developer checkout path.
- Native player packs exclude the NuGet lock file, and artifact qualification now rejects source-machine and development paths.
- Godot export restores use ignored export-specific lock files and fail qualification if any canonical dependency lock changes.
- Exact-deadline food now resolves before starvation, while a missed deadline completes its legal movement before death and only then evaluates Last Stand.
- Shield collection now occurs on the successful movement that enters its cell instead of waiting for a later render update.
- Filling the final grid cell now completes the run as a victory instead of entering an unwinnable foodless survival state.
- Packaged-player qualification now owns the launched process through exit, preventing Windows log cleanup races.
- Export-specific NuGet locks now live outside Godot's resource tree, and artifact inspection rejects packed `res://obj` metadata.
- Fallback cues now use finite PCM resources with explicit stop, detach, release, and process-exit phases, eliminating the native audio playback leak found by repeated smoke tests.
- Native editor and packaged-player smoke now fail on engine warnings, leaked objects, or a missing success marker.
- The dependency-lock command now receives the checkout root from its repository
  wrapper instead of deriving it from an installed package location, so the exact
  documented and CI command works after a non-editable install.
- Repository Python gates and screenshot capture now import the checkout source
  explicitly, preventing a non-editable CI install from hiding current code or
  rendering without checkout assets.
- Rewarded spatial near-miss events now advance the run-local achievement
  counter, while clutch and boosted-food style events remain separate signals.
- Magnet preserves food already on the next head cell and will not pull food
  onto the snake, detached hazards, or visible power-ups.
- Boost and Slow-Mo cadence factors now compose and expire independently, so
  one effect cannot clear the other.
- AI spectators target only visible collectible power-ups, and scored style
  events now read rules state instead of presentation timers.
- Food respawn reserves visible power-up cells and, on a saturated board,
  removes temporary pickups and detached hazards before declaring a true
  snake-filled grid victory.
- Unknown or malformed cosmetic unlock requirements now fail closed instead of
  granting content accidentally.
- Coverage-report filename variants are ignored, preventing local paths from
  entering a future initial commit.
- The terms-unresolved generated reference logo is preserved only in the
  ignored archive and replaced in public source by deterministic original art.
- Corrupt-save quarantine now uses one idempotent backup per save, atomically refreshing it only when the corrupt bytes change.
- PNG validation now walks the complete bounded chunk stream and verifies structure, CRCs, dimensions, image-data ordering, and terminal bytes; MP3 validation requires two complete compatible MPEG frames.
- Seven zero-byte media entries and three broken or side-effectful legacy test runners were removed.
- Placeholder assertions in gameplay integration tests were replaced with deterministic state and rendering postconditions.
- The Settings menu no longer advertises a difficulty control that has no effect.
- The locked build environment now uses setuptools 83, resolving `PYSEC-2026-3447`; the strict audit reports no known vulnerability in the resolved Python graph.
- The process entry point now propagates meaningful success and failure codes.
- QA campaign construction rejects empty scenario sets and invalid scenario
  parameters before simulation.
- Radio number keys and cycling controls no longer affect unrelated game states,
  and optional-pack absence produces one truthful status message.
- Food placement now samples uniformly from the complete bounded free-cell set
  and raises the documented full-grid exception instead of returning a sentinel.
- Source PNG validation now checks bounded decompression, decoded scanline length,
  legal filter bytes, and palette constraints in addition to chunk structure and CRCs.
- Native restoration rejects oversized dimensions, queues, counters, and tick
  values, while score arithmetic saturates instead of overflowing.
- Empty package shells and undocumented duplicate root launchers were removed.
- Rewarded near-miss, clutch, and style events now advance the run-local
  achievement counter instead of achievements always receiving zero.
- Ruleset identity validation now rejects missing, malformed, boolean, fractional,
  and non-positive versions, and content-pack compatibility derives from the same
  canonical identity used by states, replays, fixtures, and both rules runtimes.
- Phase Shift recovery no longer treats an occupied duplicate tail coordinate as a
  departing free cell, preventing survival through a real self-collision after
  overlapping body states.
- Targeted fixture generation and QA campaigns restore the caller's logger level
  after quiet generation, preventing process-global logging state from leaking
  into later tools or tests.

## 0.2.0 alpha

- Added progression, achievements, cosmetic loadouts, AI spectator channels, adaptive difficulty scaffolding, near-miss scoring, visual effects, and the expanded radio network.
- This release predates the current verification baseline. Historical completion claims are retained only in the archive.
