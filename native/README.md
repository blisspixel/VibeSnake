# Native Foundation

This directory contains the engine-independent C# foundation for the **ship product**: pure rules and persistence consumed by the Godot shell under `game/`. The Python and Pygame tree is a frozen behavior oracle for dual-runtime fixtures only - not a second runtime and not the place for new player features.

## Layout

```text
native/
|-- src/VibeSnake.Rules/          Pure deterministic rules with no Godot reference
|-- src/VibeSnake.Persistence/    Bounded replay browse/import/export/delete and atomic storage
|-- tests/VibeSnake.Rules.Tests/  xUnit rules, replay, parity, and storage contracts
|-- toolchain.json                Exact qualified tool versions and local evidence
`-- VibeSnake.slnx                Native solution

game/
|-- project.godot                 Godot application shell
|-- export_presets.cfg            Windows, Linux, and macOS export definitions
|-- VibeSnake.Game.sln            Solution required by Godot .NET export
|-- VibeSnake.Game.csproj         Godot C# project
|-- scenes/                       Scene resources
`-- scripts/                      Presentation and input adapters
```

## Commands

From the repository root:

```powershell
./scripts/test_native.ps1
./scripts/test_native_export.ps1 -GodotExecutable "C:\path\to\Godot_console.exe"
```

The first command qualifies the rules, formatting, coverage, editor import, and scene smoke. The second installs checksum-verified templates when needed, exports outside the checkout, launches the packaged player, and writes a validated SHA-256 artifact manifest. The repository-local SDK and editor are developer caches and are ignored. See [the development guide](../docs/guides/DEVELOPMENT.md) for setup and exact contracts.

## Current scope

`VibeSnake.Persistence` now includes the playback-free `AudioMixAllocator`, with bounded buses, voices, identifiers, cooldown groups, monotonic leases, per-cue polyphony, priority, stable interruption, expiry, and strongest-active music ducking. The Godot adapter uses it for a 31-cue, 8-voice SFX and 4-voice UI mix, reapplies saved bus settings after polled output changes, and retains `audio-mixing-policy-v2` engine evidence. `sfx-catalog-qualification-v1` separately proves unique combo/restart/achievement/death/power identities, exact fallback provenance/license, measured peak bounds, candidate exclusion, and rules isolation.

The Godot shell also consumes one typed multimodal presentation catalog for four hunger phases, combo score/multiplier emphasis, all nine power markers and timed/held states, explicit protection readiness, and both death causes. `multimodal-feedback-v1` locks muted, reduced-motion, flash-free, and combined fallbacks while proving the presentation qualification leaves the deterministic rules hash unchanged. Native mouse input now shares the logical action boundary through nine scaled menu targets, two-axis wheel navigation, confirm, Back, pause, and head-relative steering; `mouse-input-qualification-v1` locks the live route and letterbox rejection without changing keyboard or controller bindings.

`VisualHierarchyPolicy` is the sole production capacity and peak-priority contract for current presentation. It caps particles, per-event emission, shake sources and strength, full-screen flashes, popups, overlays, head-effect outlines, and popup text. The renderer uses it for pressure captions, protection-first outlines, a permanent head-direction marker, and terminal opacity. `visual-hierarchy-qualification-v1` verifies 3:1 gameplay foreground contrast against both board palettes and writes five deterministic, hash-bound PNG review frames without changing rules state. `candidate-accessibility-audit-v1` additionally SHA-256-binds seven packaged records across twelve exact areas, including keyboard/controller routes and remapping, audio separation, multimodal fallbacks, protective presentation profiles, and 150 percent text over eight display classes. Retained live platform pixels, physical devices, accessibility-user participation, and subjective review remain human gates.

`performance-qualification-v1` stabilizes each minimum, default, and maximum-safe effect profile for 30 unmeasured process frames before measuring 40 live frames, and publishes both counts. Its largest mixed scene fills the board with the maximum live snake that still leaves three hazards, food, and one power signal visible, then adds the complete safe particle and popup capacities. The gate publishes 60 FPS, 16.67 ms, 12 audio channels, and 2,400 logical draw submissions, enforces conservative shared-host regression ceilings, and requires identical rules hashes after 256 steps per profile. Packaged smoke runs the focused minimum-effects bare-loop burst after this full sequence, with a separate 30-frame stabilization and 40 retained samples under unchanged budgets. Named minimum-hardware measurements and driver metrics remain pending rather than being inferred from Godot's Dummy backend.

`VibeLevelDirector` is the sole authority for Grounded, Flow, Heat, Overdrive, and Transcendent escalation. It owns the exact combo thresholds and declares board, HUD, trail, particle, camera, music-layer, transition-stinger, and static accessibility budgets. Live subscribers consume typed output rather than inspecting combo milestones. `vibe-level-qualification-v1` enforces once-only transitions, critical-gameplay priority, 13 fixed scenes, seven accessibility profiles, rules and score-category isolation, and at least 4.56:1 foreground contrast. Authored music layers, camera feel, and human recognition remain pending.

`RadioCatalog` projects station and track metadata only from validated optional-pack manifests, and `OptionalPackStore` isolates invalid packs before returning a runtime catalog. The playback-free `RadioPlaybackPolicy` defines shuffle, no immediate repeat, pause/resume, station retune, end-of-track, mute/help, missing-track, and missing-pack behavior on the named radio RNG. Godot packages the exact inventory beside its managed payload, loads one verified MP3 through `RadioStreamPlayer`, exposes bounded state on menu/run/content screens, and retains `radio-behavior-qualification-v1` evidence. Actual radio packs remain blocked by `exportEligible: false`.

`BroadcastStationCatalog` and `BroadcastPolicy` define the playback-free broadcast boundary. All eight SBN stations carry complete inclusion, host, visual, short-ID, transition, and Coil metadata while remaining explicitly unapproved. Optional segments occur only at run start, major milestone, recovery, or post-run, obey cooldown and fatigue caps, yield to critical cues, retain captions without audio, and select host IDs through per-station no-repeat bags. Radio tracks now use per-station shuffle bags that exhaust playable tracks before refill and avoid a refill-boundary repeat. `broadcast-qualification-v1` proves those policies, adaptive-layer refusal, and radio/gameplay RNG plus rules-state isolation without claiming authored content approval.

`RunModeCatalog` freezes `classic@1` and `vibe@1` as separate product contracts over `vibesnake-core@4`. Each mode owns its description, 64 by 33 board, fresh local seed, pause, restart, adaptive state, score model, difficulty policy, and score category. `sha256-canonical-runconfig-v3` includes mode, score switches, and the DDA contract. Classic genuinely disables hunger, combo and bonus scoring, near misses, powers, progression candidates, and adaptation. Vibe enables deterministic `vibe-bounded-hunger-v1` by default and exposes a persisted opt-out; enabled and disabled runs use separate score categories. The Godot menu selects either mode through remappable keyboard/controller actions, restart retains it, `mode-contract-qualification-v2` proves exact mechanics, and `adaptive-fairness-qualification-v1` proves bounds, disclosure, replay safety, and category isolation.

The deterministic balance laboratory adds nine versioned policies, ten hostile scenario probes, reviewed fixed/exploratory/failure seed corpora, paired state-hash comparison, and verified outlier replay capture. Its separate observed baseline runs 100 fixed seeds through every policy for Classic, Vibe DDA-on, and Vibe DDA-off, retaining 2,700 per-run metrics and 27 hash-locked distributions. Six policies are marked reference AI; stress and replay instruments are not. The baseline is explicitly not a human target or a claim about fun.

`AiPersonalityCatalog` retains ten frozen-oracle compatibility IDs behind reviewed broadcast identities and deterministic integer traits. `native-personality-controller-v2` consumes every trait without engine or persistence dependencies. The native AI league runs all ten definitions over the same twelve reviewed QA seeds, mirrors 98,984 rules steps, records seven behavior metric families by rules version, and proves all 60 same-state opposite-extreme interventions exceed the 1 percent materiality floor. One measured claim per built-in passes. The strict shared custom schema returns source-specific failures and labels valid custom content `CUSTOM / UNOFFICIAL`; typed snapshots drive the live compact spectator ticker with target, risk, policy, current reason, and recent decisions. Every run uses the closed noncompetitive AI score context and never enters human score storage.

`ProgressionCatalog`, `CosmeticSetCatalog`, `BroadcastTourCatalog`, and `BroadcastTourSession` form the V080-04 product progression boundary. Twenty exact goals span mastery, discovery, identity, and early/middle/mastery pacing. Eight curated expression-only cosmetic sets retain head recognition, non-color identity, and trails capped at 50 percent opacity. Twelve fixed-seed Tour events span four dependency-gated tiers with canonical rival/station references, exact primary/style goals, replayable practice runs, and immediate rematches. `ProgressionDocument` stores monotonic canonical-human metrics, one highlighted goal, exact earned rewards, selected and saved cosmetic sets, and dependency-closed event completion. The Godot goal, Tour, and cosmetics screens are complete for logical keyboard and controller routes, while real-player pacing and visual review remain open.

The V080-05 curation handoff binds all 95 runtime-radio asset IDs to eight exact station review queues without approving or deleting source audio. Candidate inventories range from 11 to 13 tracks, radio duplicates and temporary/test names are absent, and current rights plus structural integrity are complete. `ContentCreditsDocument` renders stable human-readable credits and third-party notices only from validated manifests, with no timestamp or machine path. The evidence retains zero core-music candidates, approved tracks, technical listening reviews, production manifests, and export-eligible files until those human and media gates are genuinely complete.

`ValidateCreatorContent` completes the V080-06 creator-validation boundary. Its `personality` and `pack-set` commands return stable schema-1 JSON without loading payload code. Closed schemas, examples, sixteen personality codes, fifteen pack/compatibility codes, and collision rules are published. One core resolves first; unique optional IDs resolve ordinally; duplicate IDs fail instead of overriding. Qualification prohibits process launch, reflection/native/dynamic loading, scripting, network references, and engine references, while arbitrary code plugins remain outside 1.0.

`RepositoryChecks` owns the first thirteen V030-13 validator-retirement slices. Its `achievement-candidates`, `achievement-candidates-write`, `docs`, `version`, `source`, `freeze`, `freeze-baseline`, `locks`, `lock-write`, `logo`, `badges`, `badge-write`, `inventory`, `inventory-write`, `inventory-release`, `screenshots`, `screenshots-write`, `materials`, `materials-write`, `materials-candidate`, `rehearsal`, `rehearsal-write`, `rehearsal-record`, `stable`, `stable-write`, `stable-record`, `plugin`, and combined `all` commands validate the frozen reviewed achievement-candidate corpus, canonical Markdown links, unique changelog contract releases, exact UTF-8 SemVer identity, native application identity, temporary Python package spelling, active authored-file scope, credentials and signing material, unfinished markers, prohibited Unicode, attribution policy, Python placeholders, the complete candidate-freeze policy, resolved frozen surfaces, deterministic SHA-256 baselines, both Python dependency-lock profiles, the approved project-logo PNG identity, exact canonical station-badge bytes, deterministic source-content inventory, staged README screenshots, the release-material foundation and structurally complete candidate record, the release-rehearsal foundation and retained external-attestation record, the stable-promotion foundation and retained protected-workflow record, and bounded source and packaged Agent Plugin trees. Lock generation requires exact `uv 0.11.33`, prefers the checkout environment, treats warnings and timeouts as failures, and replaces validated LF output atomically. Badge generation uses closed definitions, project-owned glyphs, integer-only drawing, a canonical RGB PNG encoder, link-safe bounded paths, and atomic per-file replacement. Inventory generation uses strict fixed schemas, bounded link-free traversal, exact classification, streaming hashes, JSON, Markdown, CSV, WAV, MPEG, and full PNG container plus decoded-scanline validation, duplicate relationships, release blockers, canonical LF JSON, and atomic replacement. Independent zlib header and Adler-32 checks reject incomplete PNG streams that a decoder can otherwise accept after producing all expected scanlines. Screenshot verification uses that full PNG parser, a closed canonical manifest, strict source fingerprinting, exact hashes, dimensions, and README references. Capture receives an explicit pinned Godot executable, uses separate temporary output and user data, validates the complete set before replacement, and writes the manifest last. Release-material validation uses strict duplicate-free JSON, bounded regular files and link-free containment, exact contract and document identities, closed candidate fields, declared positive platform sizes, bounded PNG/JPEG/MP4/WebM container checks, stable retained-file snapshots and hashes, evidence-bound claims, canonical LF `release-materials-handoff-v2` output, and atomic replacement. A passing candidate sets `candidateMaterialComplete: true` while keeping `releaseAcceptance` false pending artifact reconciliation, claim approval, visible image review, and video playback. Release-rehearsal validation binds a checked-in material-acceptance authority, exact v1 protocol and record shapes, three candidate, previous, and manifest file sets, 33 passing operation cells, protected-data preservation, withdrawal, four authority roles, and exact retained closure into canonical LF `release-rehearsal-handoff-v2` output. It validates a retained external attestation and never performs an install, approval, withdrawal, or publication. Stable-promotion validation binds the protected rebuild contract to a second checked-in authority that closes ten upstream IDs, exact accepted kinds, ordered gates, eight generic decision schemas, and the special material and rehearsal schemas. It cross-checks revision and version, then requires nine review decisions to bind the unsigned artifact and manifest cohort. The platform-signing decision binds that cohort as its input and the byte-changing signed public artifacts, manifests, and provenance as its result. Optional content, protected workflow identity, public-install results, preservation evidence, and complete retained hashes are also exact before canonical LF `stable-promotion-handoff-v2` output. The foundation can qualify without claiming a protected run. Protected execution and release acceptance remain pending, and the validator never reviews, signs, tags, installs, uploads, or publishes. Achievement-candidate rendering preserves four reviewed Python-origin vectors as exact canonical LF bytes without executing native rules, while the existing C# parity test remains the independent live consumer. Its fixed-path writer rejects links and portable aliases, bounds reads and sibling discovery, replaces atomically, and verifies the result. Agent Plugin validation requires canonical metadata, a closed minimal-skill profile, strict JSON, link and launch containment, exact packaged components, and complete streaming checksums. Ruff remains the complete Python syntax parser while the frozen oracle exists. The 240 focused repository-check contracts cover malformed files, duplicate declarations, drift, encoded links, fenced examples, missing targets, invalid UTF-8, excluded trees, every Unicode range, credential names, inline and multiline placeholders, ignored strings and comments, malformed lexical structure, unsafe or empty freeze patterns, overlapping owners, generated-tree exclusions, activation prerequisites, manifest tampering, ordered lock input identity, exact pins and hashes, resolver failure modes, atomic output, plugin shape and containment, canonical PNG format and identity, valid metadata-only drift, bounded badge layouts, exact inventory snapshots, policy ambiguity, corrupt media containers and streams, screenshot staging and freshness, accepted-material, rehearsal, stable-promotion cross-binding, exact frozen fixture identity, noncanonical bytes, path aliases, and link-safe atomic fixture replacement, tree and byte limits, release readiness, deterministic diagnostics, and stable command routes.

`ShellLocalization` completes the automated V080-07 localization boundary with 734 stable English IDs and 114 exact named-parameter templates across thirteen supported shell flows plus preview-only Agent Arena watch copy. The opt-in `qps-ploc` development locale accents copy, preserves input-glyph parameters, expands every catalog result by at least 1.3125, and passes fallback-font glyph plus logical-canvas layout checks at 150 percent text, including the seven composed Agent Arena overlay rows under worst-case content. Rules onboarding and Persistence broadcast scheduling expose stable IDs instead of presentation strings, feedback events carry typed localization references, all 18 onboarding, 24 feedback, and 24 broadcast caption IDs resolve, and retained evidence requires zero direct draw, prompt, static status, composed status, or audited domain-presentation expressions. Visible multi-platform review remains required before release closure.

The post-1.0 Agent Arena source preview uses Agent Host version 0.18.0 and Agent Plugin version 0.18.0, `AgentPassportV4`, and the fixed `symbolic-step-v4` profile with caller-declared bounded identity and closed avatar, accent, and station catalogs. Observation, result, and host response DTO v5 plus viewer frame v9 reject legacy, mixed, unknown-catalog, identity-drifting, lesson-contradicting, style-contradicting, and replay-identity-drifting payloads. Frame v9 carries `vibesnake-agent-survival-state-v1`, which the viewer recomputes from the public observation and rejects on any disagreement. Rules resource v19, identity resource v3, style catalog resource v3, and Signal School resource v4 expose the current public catalogs. Rules resource v19 adds the published `lifecycle_semantics`, `argument_binding`, `receipt`, `survival_state`, `archive`, `passport`, `story`, and `qualification` vocabulary. The AA-06 `vibesnake-agent-exhibition-receipt-v2` hash-links both verified lane replays, a closed division identity, the passport, the replay-derived style and lesson evidence, and the ordered accepted presentation events into an instance `receipt_hash`, and publishes a rematch-stable `route_identity_hash`; presentation display time stays outside both. `archive_exhibition` keeps one verified exhibition in a bounded 32-entry, four-megabyte local archive beside the saved replay file name of every lane it contains, writing atomically, evicting the oldest at capacity and naming every exhibition it drops, refusing to overwrite a different exhibition under an existing receipt hash, and quarantining an unreadable or self-inconsistent document rather than repairing it. `list_exhibitions` reads that archive without writing and can narrow to one route identity, and `forget_exhibition` removes one exhibition or clears the store. Stored schema 2 migrates losslessly from schema 1 on read, and every archive response publishes the bytes on disk, the bytes the next write would produce, both remaining bounds, and each entry's position in the store. The archive lives outside the supported player Persistence assembly and holds no human score, progression, or profile data. `record_passport` folds one verified receipt into a separate `vibesnake-agent-passport-document-v1` store of at most 16 agents and 1,048,576 bytes; `list_passports` reads it without writing; `forget_passport` removes one record or clears the store. A passport record stores only facts the receipts earned and never a display name. `get_qualification_report` classifies archived receipts against the public decks: voluntary `finish_match` is never a qualifying result, completed practice is not a standing, and standings never mix divisions. `get_exhibition_story` reconstructs first-crossing style and lesson highlights from the named agent tape, and a lane switch in playback is a cut rather than an interpolation from one snake to the other. Signal School definition v2, progress v3, delta v2, and outcome v3 publish exactly eight ordered lessons with exactly two ordered factual requirements each. Once live progress satisfies every requirement it recommends `finish_match`. Successful lesson finalization independently verifies replay facts and the separate maximum-32 valid-state opposite-reversal witness chain, then binds replay and attempt evidence hashes into an outcome with lifecycle `completed` and no retry descriptor; partial lessons and ordinary nonterminal early finishes remain `aborted`, and only incomplete or failed-closed practice provides fresh-session retry guidance. Exact retries add no witness. The `death-read` instruction uses a deterministic self-collision route because starvation exceeds its cap. Missing, unexpected, wrong-typed, and out-of-contract arguments are rejected on every tool before method binding with the exact mismatch and no match-state change; `gameplaySeed` is a quoted decimal string, and an out-of-contract value names the accepted vocabulary or range, the resource that publishes it, and the value that arrived. Each Style Contract publishes exactly two ordered factual criteria with closed units and comparisons. Live values are recomputed from rules-advanced steps and may rise or fall; v3 names their booleans and counts as threshold crossings rather than grades. Only successful finalization returns a style outcome independently reconstructed from and hash-bound to the verified replay. Agent Watch passes the resolved passport avatar into body, trail, accessory, and head-marker rendering without reading or changing the watching human's cosmetic progression; the station catalog owns presentation names while broadcast approval remains separate. Practice completion is factual evidence, not mastery. Human identity legibility and spectator appeal remain open.

V080-08 clean capture uses the same remappable Help action on keyboard and controller. Gameplay and replay capture can hide run HUD, replay controls, terminal, audio-status, debug, and spectator overlay families while deterministic playback, speed, seeking, and rules hashes remain unchanged. Each explicit verified replay export also writes an atomic, idempotent `vibesnake-run-capture-summary-v1` sidecar with exactly 24 closed fields. Application, rules, mode, score-category, config, replay-integrity, seed, and outcome metadata are retained; player identity, arbitrary text, and private paths are absent. `capture-sharing-qualification-v1` is automated, while retained platform captures and final trailer review remain human gates.

The rules kernel currently proves seeded PCG32 randomness, bounded direction input, fixed-step wraparound movement, food placement and growth, combo interpolation, speed and length scoring, exact starvation, collision precedence, grid completion, pure C# contracts for all nine powers (Shield, Phase Shift, Last Stand, Slow-Mo, Boost, Magnet, Bait, Gluttony, Segment Detach), `RulesCadenceClock` wall-clock tempo intervals, immutable ordered events, explicit restart, detached snapshots, canonical JSON schema 3 serialization and strict restoration (session achievement counters included), explicit `vibesnake-core@4` identity, schema 1 replay compatibility, resource-bounded verification, clock-free deterministic replay playback, and `fnv1a64-canonical-json-v4` state hashes. Shared Python-to-C# fixtures cover movement, core rules, Shield, Phase Shift, Last Stand, remaining powers, and the achievement-candidate product path, with automated delta reduction on failing movement prefixes. Generated operation campaigns repeatedly restore and continue runs across terminal and restart boundaries. The live recorder preserves rejected input attempts, checks each Godot step against a private rules mirror, and fails closed on divergence or bounds. The persistence assembly owns schema 1 AchievementsStore for permanent run-local unlock IDs and verifies, serializes decisions across processes, atomically stores and exports, strictly reloads, projects complete browser metadata/status, prepares stale-safe content-hashed deletion consent, and read-only inspects replays without adding files or clocks to the rules kernel. It also owns pure audio-output retry/transition policy and the strict schema 1 content-pack parser/resolver with exact inventory, metadata, rights, credit, dependency, compatibility, station, canonical encoding, and optional-failure isolation contracts. The Godot shell proves logical keyboard and any-controller defaults, focus-loss pause safety, full nine-power markers, composite HUD, head outlines, hazards, multi-power captions, Slow-Mo/Boost cadence drain, 31 finite fallback cues with retained identity/mixing/stress/recovery evidence, core-only offline play with optional absence/removal/tamper/incompatibility/duplicate isolation, replay recording, verified background metadata/status browsing, 0.5x through 4x playback, HUD toggle, atomic export, exact deletion cancel/confirm, lossless terminal-save queuing, run-start gating, save-aware quit, bounded drop import, and that the .NET 10 assemblies can build, run, export, and launch inside the pinned engine line. Host-dependent pure rules throughput evidence is written to `TestResults/native/rules_throughput.json`. Artifact inspection fails closed when a package contains inventory paths that are not export-eligible. Native export qualification launches the packaged player from a read-only install with fresh external user data and logs, exercises spaces and non-ASCII characters in all three paths, rejects an adjacent write probe, and verifies the complete installed-file digest remains unchanged. Hosted Windows x64, macOS Universal, and Linux x64 Debug bundles pass required Rules, Persistence, and Game payload, portability, no-Python, no-export-lock, engine-warning, leaked-object, and complete hash-inventory checks; a Windows x64 Release bundle also passes locally.

The native persistence surface also owns atomic onboarding decisions, schema-2 bounded personal bests with explicit mode/run-kind/seed/difficulty/DDA identity and visible schema-1 `Legacy 0.2` migration, strict schema-1 `progression.json`, four fixed source-preserving household ghost slots, atomic privacy-safe run cards, and `PlayerDataRecoveryService`. Reset plans are fixed to preferences/bindings, progression, personal bests, replays, or optional content; the replay category includes stored replays, exports, and offline challenges. Confirmed work creates a strict bounded SHA-256 backup before removal, while inspection and restore reject corruption, unsafe entries, category/path mismatches, interrupted staging, and overwrite conflicts. The Godot shell exposes this through keyboard/controller Data settings and retains `run-end-qualification-v1`, `score-identity-qualification-v1`, `player-data-recovery-qualification-v1`, `progression-qualification-v1`, and `offline-comparison-qualification-v1` evidence.

This is the live product kernel, not final store polish. Already in tree: near-miss and combo-expired default-on rules, achievement candidates (product-gated) with permanent unlock store and Godot browse UI, multi-power synergy campaigns, schema 3 session counters, architecture purity bans, content eligibility reports, persisted keyboard/controller remap capture with explicit conflict swap/cancel, deliberate-input Xbox/PlayStation/Nintendo/generic vector prompt families with text fallback, a nine-case keyboard/D-pad/stick render-cadence matrix, and a six-section, 34-row settings screen with raw keyboard/controller completion, schema-7 preferences, a persisted bounded stick deadzone, independent D-pad fallback, onboarding, ordered run-end/personal-best presentation, separated reset/recovery, default-off local playtest consent with local export and deletion, and complete automated V080-04 goal/Tour/cosmetic flows. Centralized shell palette/font, contrast, 150-percent text-layout, non-color marker, and long-catalog pagination evidence; required minimum/16:9/4:3/16:10/ultrawide/square/4K/5K viewport evidence; recoverable optional-pack lifecycle; strict release signing-readiness; deterministic direct-download/store-depot qualification shapes; bounded verified replay metadata/status browsing, playback, export, and exact deletion consent; interactive ten-rival AI channels with local standings and exact-seed challenges; optional lore; and stable seed codes, equal-rules ghosts, household slots, and privacy-safe run cards also pass. Still open for later packages: authored audio and radio packs, pack export eligibility (`exportEligible` still zero), physical-controller evidence, retained platform scaling screenshots, presentation frame-time measurement on declared hardware, selected-store integration, protected platform signing, and production presentation feel. macOS and Linux artifact smokes exist in hosted CI; store-ready promotion and actual platform signing do not. Prefer new work here and in `game/`, not in Python. See [the playtest summary contract](../docs/design/PLAYTEST_SUMMARIES.md), [the replay contract](../docs/engineering/REPLAYS.md), [user-data directories](../docs/engineering/USER_DATA.md), [release signing](../docs/release/SIGNING.md), and [roadmap Product path](../ROADMAP.md#product-path-read-this-first).
